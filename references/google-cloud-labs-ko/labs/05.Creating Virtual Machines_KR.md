# 05. Creating Virtual Machines

> [← 이전 실습](04.Implement%20Private%20Google%20Access%20and%20Cloud%20NAT_KR.md) · [📚 전체 목록](../README.md) · [다음 실습 →](06.Working%20with%20Virtual%20Machines_KR.md)

## Overview (개요)

이 실습에서는 Virtual Machine 인스턴스의 다양한 옵션을 살펴보고, 서로 다른 특성을 가진 여러 VM을 생성해봅니다.

## Objectives (목표)

이 실습에서는 VM에 사용할 수 있는 옵션들을 살펴보고, 위치(location)에 따른 차이를 확인합니다.

이 실습에서는 다음 작업을 수행하는 방법을 배웁니다.

- 여러 개의 standard VM을 생성합니다.
- 고급(advanced) VM을 생성합니다.

---

---

## 🧩 Task 1. 유틸리티 가상 머신 생성하기

### VM 생성하기

1. Google Cloud console에서 **Navigation menu(탐색 메뉴)**의 **Compute Engine(컴퓨트 엔진) > VM instances(VM 인스턴스)**를 클릭합니다.
2. **Create Instance(인스턴스 만들기)**를 클릭합니다.
3. 다음과 같이 지정하고 나머지는 기본값으로 둡니다.

   | Property (속성) | Value (값) |
   |---|---|
   | Name | `utility-vm` (또는 원하는 이름) |
   | Region | `us-central1` (또는 지정된 리전) |
   | Zone | `us-central1-c` (또는 지정된 존) |
   | Series | E2 |
4. **Machine type(머신 유형)**에서 여러 옵션을 살펴봅니다.

   > **💡 참고:** 메뉴에는 vCPU 개수, 메모리 용량, 그리고 `e2-medium` 같은 심볼릭 이름이 함께 표시됩니다. 이 심볼릭 이름은 `gcloud` 명령어로 VM을 생성할 때 머신 유형을 선택하는 매개변수로 사용됩니다. 존(zone)과 머신 유형 오른쪽에는 선택한 머신 유형에 대한 월별 예상 비용 내역이 표시됩니다.

5. **Machine type(머신 유형)**에서 **standard > e2-standard-4 (4 vCPUs, 16 GB memory)**를 선택합니다. 비용이 어떻게 달라졌나요?
6. **Machine type(머신 유형)**에서 **Shared-core > e2-medium (2 vCPUs, 4 GB memory)**를 선택합니다.
7. **Networking(네트워킹)**을 클릭합니다.
8. **Network interfaces(네트워크 인터페이스)**에서 드롭다운 아이콘을 클릭합니다.
9. **External IPv4 address(외부 IPv4 주소)**에서 **None**을 선택합니다.
10. **Done(완료)**을 클릭합니다.
11. 나머지 설정은 모두 기본값으로 두고 **Create(만들기)**를 클릭합니다. 새 VM이 생성될 때까지 기다립니다.

> **💡 참고:** Google Cloud Free Program의 [Free Tier](https://cloud.google.com/free/docs/gcp-free-tier#always-free-usage-limits)에 해당하지 않는 외부 IP 주소에는 소액의 비용이 발생합니다. 자세한 요금 정보는 Virtual Private Cloud Guide의 [External IP address pricing section](https://cloud.google.com/compute/network-pricing#ipaddress)에서 확인할 수 있습니다.

### VM 세부정보 살펴보기

1. **VM instances(VM 인스턴스)** 페이지에서 방금 만든 VM의 이름을 클릭합니다.
2. **CPU platform** 값을 확인합니다. **Edit(수정)**를 클릭합니다.

   > **💡 참고:** 머신 유형, CPU 플랫폼, 존(zone)은 변경할 수 없다는 점에 유의하세요.

   네트워크 태그를 추가하고 방화벽을 통해 인터넷에서 특정 네트워크 트래픽을 허용할 수 있습니다. VM의 일부 속성은 VM 자체에 통합되어 있어서 VM이 생성될 때 결정되며 이후에는 변경할 수 없습니다. 다른 속성들은 편집할 수 있습니다.

   추가 디스크를 붙일 수 있으며, 인스턴스가 삭제될 때 부팅 디스크(boot disk)도 함께 삭제될지 여부도 결정할 수 있습니다.

   일반적으로 부팅 디스크는 인스턴스가 삭제될 때 자동으로 함께 삭제되도록 기본 설정되어 있습니다. 하지만 이 동작을 재정의해야 할 때도 있습니다. 이 기능은 매우 중요한데, 부팅 디스크가 실행 중인 인스턴스에 연결되어 있는 동안에는 해당 디스크로부터 이미지를 생성할 수 없기 때문입니다.

   따라서 부팅 디스크로부터 시스템 이미지를 생성하려면 **Delete boot disk when instance is deleted** 옵션을 비활성화해야 합니다.

3. **Cancel(취소)**을 클릭합니다.
4. **Availability policies**를 살펴봅니다.

   > **💡 참고:** 프리엠프티블(preemptible)이 아닌 인스턴스를 프리엠프티블 인스턴스로 전환할 수는 없습니다. 이 선택은 VM 생성 시점에 이루어져야 합니다. 프리엠프티블 인스턴스는 언제든 중단될 수 있지만 더 저렴한 비용으로 이용할 수 있습니다.

   VM이 어떤 이유로든(예: 장애나 하드웨어 오류) 중지되면, 자동 재시작 기능이 VM을 다시 시작합니다. 이것이 원하는 동작인가요? 애플리케이션이 멱등성(idempotent, 재시작을 제대로 처리하도록 작성됨)을 가지고 있나요?

   호스트 유지보수 작업 중에는 VM이 라이브 마이그레이션(live migration)되도록 설정되어 있습니다. 다만 마이그레이션 대신 VM을 종료(terminate)하도록 설정할 수도 있습니다.

   변경 사항을 적용하면, 특히 방화벽 추가나 외부 IP 변경 같은 네트워킹 관련 변경일 경우 반영되기까지 몇 분 정도 걸릴 수 있습니다.

### VM 로그 살펴보기

1. VM의 **VM instance details(VM 인스턴스 세부정보)** 페이지에서 **Logging(로깅)**을 클릭합니다.

   > **💡 참고:** 이제 Logging 페이지로 이동한 것을 확인할 수 있습니다. 이는 구조화된 로그 뷰(structured log view)입니다. 상단의 풀다운 메뉴로 필터링할 수 있고, 라벨이나 텍스트로 검색할 수 있는 검색창도 있습니다.

2. 한 줄 왼쪽의 **Expand this log entry** 아이콘을 클릭하여 해당 로그가 어떤 정보를 담고 있는지 확인합니다.

---

---

## 🧩 Task 2. Windows 가상 머신 생성하기

### VM 생성하기

1. Google Cloud console에서 **Navigation menu(탐색 메뉴)**의 **Compute Engine(컴퓨트 엔진) > VM instances(VM 인스턴스)**를 클릭합니다.
2. **Create instance(인스턴스 만들기)**를 클릭합니다.
3. 다음과 같이 지정합니다.

   | Property (속성) | Value (값) |
   |---|---|
   | Name | `windows-vm` (또는 원하는 이름) |
   | Region | `us-central1` (또는 지정된 리전) |
   | Zone | `us-central1-c` (또는 지정된 존) |

4. **Series(시리즈)**에서 **E2**를 선택합니다.
5. **Machine type(머신 유형)**에서 **standard > e2-standard-2 (2 vCPUs, 8 GB memory)**를 선택합니다.
6. **OS and storage(OS 및 스토리지)**를 클릭한 다음 **Change(변경)**를 클릭합니다.
7. **Operating system(운영체제)**에서 **Windows Server**를 선택합니다.
8. **Version(버전)**에서 **Windows Server 2022 Datacenter Core** (또는 2019/2016)를 선택합니다.
9. **Boot disk type**에서 **SSD persistent disk**를 선택합니다.
10. **Size (GB)**에 `64`를 입력합니다.
11. **Select(선택)**를 클릭합니다.
12. **Networking(네트워킹)**을 클릭합니다.
13. **Firewall(방화벽)**에서 **Allow HTTP traffic**과 **Allow HTTPS traffic**을 활성화합니다.
14. **Create(만들기)**를 클릭합니다.

> **💡 참고:** VM이 실행되면, 맨 오른쪽 열의 연결 옵션이 SSH가 아니라 RDP로 표시되는 것을 확인할 수 있습니다. RDP는 Remote Desktop Protocol의 약자입니다. Windows 데스크톱에 연결하려면 로컬 컴퓨터에 RDP 클라이언트가 설치되어 있어야 합니다.

> **💡 참고:** 로컬 컴퓨터에 RDP 클라이언트를 설치하는 것은 이 실습과 이 과정의 범위를 벗어납니다. 그렇기 때문에 이 실습에서는 Windows VM에 실제로 연결하지는 않습니다. 다만 RDP 클라이언트가 필요한 시점까지의 일반적인 절차는 그대로 따라가 봅니다. Windows VM에 연결하는 방법은 [Connecting to Windows VMs Guide](https://cloud.google.com/compute/docs/instances/windows/connecting-to-windows-instance)에서 확인할 수 있습니다.

### VM의 비밀번호 설정하기

1. Windows VM의 이름을 클릭하여 **VM instance details(VM 인스턴스 세부정보)**에 접근합니다.
2. 이 Windows VM에는 아직 유효한 비밀번호가 없습니다. 비밀번호 없이는 Windows VM에 로그인할 수 없습니다. **Set Windows password**를 클릭합니다.
3. **Set**을 클릭합니다.
4. 제공된 비밀번호를 복사하고 **CLOSE**를 클릭합니다.

> **💡 참고:** 이 실습에서는 Windows VM에 **연결하지 않습니다**. 하지만 실제로 연결한다면 (설치한 RDP 클라이언트에 따라) 대략 다음과 같은 과정을 거치게 됩니다. 여기서 소개하는 RDP 클라이언트는 [Chrome webstore](https://chrome.google.com/webstore/detail/chrome-rdp-for-google-clo/mpbbnannobiobpnfblimoapbephgifkm?hl=en-US)에서 Chrome용으로 설치할 수 있습니다. **VM instances(VM 인스턴스)** 페이지에서 Windows VM의 **RDP**를 클릭하고, 앞서 복사해둔 비밀번호로 연결하면 됩니다.

---

---

## 🧩 Task 3. 커스텀 가상 머신 생성하기

### VM 생성하기

1. Google Cloud console에서 **Navigation menu(탐색 메뉴)**의 **Compute Engine(컴퓨트 엔진) > VM instances(VM 인스턴스)**를 클릭합니다.
2. **Create instance(인스턴스 만들기)**를 클릭합니다.
3. 다음과 같이 지정합니다.

   | Property (속성) | Value (값) |
   |---|---|
   | Name | `custom-vm` (또는 원하는 이름) |
   | Region | `us-central1` (또는 지정된 리전) |
   | Zone | `us-central1-c` (또는 지정된 존) |

4. **Series(시리즈)**에서 **E2**를 선택합니다.
5. **Machine type(머신 유형)**에서 **Custom(커스텀)**을 클릭합니다.
6. **Cores**에 `2`를 입력합니다.
7. **Memory**에 `4 GB`를 입력합니다.
8. **OS and storage(OS 및 스토리지)**를 클릭합니다.
9. **Image(이미지)**가 **Debian GNU/Linux 12 (bookworm)**이 아니라면, **Change(변경)**를 클릭하여 **Debian GNU/Linux 12 (bookworm)**을 선택한 다음 **Select(선택)**를 클릭합니다.
10. **Create(만들기)**를 클릭합니다.

### 커스텀 VM에 SSH로 접속하기

1. 방금 생성한 커스텀 VM에서 **SSH(SSH 접속)**를 클릭합니다.
2. 커스텀 VM의 사용/미사용 메모리와 스왑(swap) 공간에 대한 정보를 확인하려면 다음 명령어를 실행합니다.

   ```bash
   free
   ```

3. VM에 설치된 RAM에 대한 세부정보를 확인하려면 다음 명령어를 실행합니다.

   ```bash
   sudo dmidecode -t 17
   ```

4. 프로세서 개수를 확인하려면 다음 명령어를 실행합니다.

   ```bash
   nproc
   ```

5. VM에 설치된 CPU에 대한 세부정보를 확인하려면 다음 명령어를 실행합니다.

   ```bash
   lscpu
   ```

6. SSH 터미널을 종료하려면 다음 명령어를 실행합니다.

   ```bash
   exit
   ```

---

---

## 🧩 Task 4. Review (검토)

이 실습에서는 서로 다른 유형과 특성을 가진 여러 가상 머신 인스턴스를 생성했습니다. 그중 하나는 관리 목적의 작은 유틸리티 VM이었습니다. 또한 standard VM과 custom VM도 생성했습니다. Windows VM과 Linux VM을 모두 실행해보았고, VM을 삭제하기도 했습니다.

---

## End your lab (실습 종료)

---

> [← 이전 실습](04.Implement%20Private%20Google%20Access%20and%20Cloud%20NAT_KR.md) · [📚 전체 목록](../README.md) · [다음 실습 →](06.Working%20with%20Virtual%20Machines_KR.md)
