# 07. Exploring IAM

> [← 이전 실습](06.Working%20with%20Virtual%20Machines_KR.md) · [📚 전체 목록](../README.md) · [다음 실습 →](08.Cloud%20Storage_KR.md)

## Overview (개요)

이 실습에서는 **Service Account User(서비스 계정 사용자)** 역할을 사용하는 방법과 역할을 부여하는 방법을 배웁니다.

## Objectives (목표)

이 실습에서는 다음 작업을 수행하는 방법을 배웁니다.

- IAM을 사용하여 액세스 제어를 구현합니다.
- 특정 기능이나 리소스에 대한 액세스를 제한합니다.
- **Service Account User(서비스 계정 사용자)** 역할을 사용합니다.

---

---

## 🧩 Task 1. 두 사용자를 위한 설정하기

이 작업에서는 **Username 1**과 **Username 2**라는 두 사용자를 위해 실습을 설정합니다.

### 첫 번째 사용자로 Cloud console에 로그인하기

1. 이 실습에서는 **Connection Details(연결 세부정보)** 대화상자에서 확인할 수 있는 두 개의 사용자 이름을 제공합니다. 평소처럼 시크릿(Incognito) 창에서 Qwiklabs가 제공한 **Username 1**로 Cloud console에 로그인합니다. 두 사용자 이름은 동일한 하나의 비밀번호를 사용한다는 점에 유의하세요.

### 두 번째 사용자로 Cloud console에 로그인하기

1. 시크릿 창에서 새 탭을 엽니다.
2. [console.cloud.google.com](https://console.cloud.google.com/)으로 이동합니다.
3. 화면 오른쪽 상단의 사용자 아이콘을 클릭한 다음 **Add account(계정 추가)**를 클릭합니다.
4. Qwiklabs가 제공한 **Username 2**로 Cloud console에 로그인합니다.

> **💡 참고:** 실습 도중 **Username 1** 계정에서 로그아웃하면 **Username 2** 계정이 Qwiklabs에 의해 삭제됩니다. 따라서 **Username 2**를 다 사용할 때까지는 **Username 1**에 로그인된 상태를 유지하세요.

---

---

## 🧩 Task 2. IAM 콘솔 살펴보기

이 작업에서는 **Username 1**과 **Username 2** 자격 증명을 사용하여 IAM 콘솔을 살펴봅니다.

**Username 1** Cloud console 탭에 있는지 확인하세요.

### IAM 콘솔로 이동하여 역할 살펴보기

1. Google Cloud console에서 **Navigation menu(탐색 메뉴)**의 **IAM & admin > IAM**을 클릭합니다.
2. **Grant Access(액세스 권한 부여)**를 클릭하고 드롭다운 메뉴에서 역할들을 살펴봅니다. **Roles** 메뉴를 탐색하며 각 리소스와 연관된 다양한 역할을 확인하세요.
3. **CANCEL**을 클릭합니다.
4. **Username 2** Cloud console 탭으로 전환합니다.
5. **Navigation menu(탐색 메뉴)**의 **IAM & admin > IAM**을 클릭합니다. Qwiklabs **Connection Details(연결 세부정보)** 대화상자에 나온 **Username 1**과 **Username 2**의 이름이 있는 줄을 목록에서 찾아봅니다.

> **💡 참고:** **Username 2**는 현재 프로젝트에 대한 액세스 권한이 있지만 Project Owner 역할은 없기 때문에 어떤 역할도 수정할 수 없습니다. **Username 2**의 연필 아이콘 위에 마우스를 올려서 이를 확인해 보세요.

6. **Username 1** Cloud console 탭으로 다시 전환합니다.
7. IAM 콘솔에서 **Username 2**의 연필 아이콘을 클릭합니다. **Username 2**는 현재 **Viewer(뷰어)** 역할을 가지고 있습니다. Project Role은 변경하지 마세요.
8. **CANCEL**을 클릭합니다.

---

---

## 🧩 Task 3. 액세스 테스트를 위한 리소스 준비하기

이 작업에서는 Cloud Storage 버킷을 생성합니다. 그런 다음 로컬 머신에서 샘플 파일을 Cloud Storage 버킷에 업로드합니다. 마지막으로 **Username 2**가 해당 버킷을 볼 수 있는지 확인합니다.

### 버킷을 생성하고 샘플 파일 업로드하기

1. 아직 **Username 1** Cloud console 탭에 있지 않다면 전환합니다.
2. Google Cloud console에서 **Navigation menu(탐색 메뉴)**의 **Cloud Storage(클라우드 스토리지) > Buckets(버킷)**를 클릭합니다.
3. **+Create**를 클릭합니다.
4. 다음과 같이 지정하고 나머지는 기본값으로 둡니다.

   | Property | Value (type value or select option as specified) |
   |---|---|
   | Name | 전역적으로 고유한 이름을 입력합니다 |
   | Location type | Multi-region |

   > **💡 참고:** 버킷 이름을 기록해 두세요. 이후 단계에서 `[YOUR_BUCKET_NAME]`으로 참조됩니다.

5. **CREATE**를 클릭합니다.

   > **💡 참고:** **Public access will be prevented**라는 메시지가 표시되고 **Enforce public access prevention on this bucket** 옵션이 선택되어 있다면 **Confirm(확인)**을 클릭합니다.

6. **Upload(업로드)**를 클릭하고 드롭다운에서 **Upload file**을 선택합니다.
7. 로컬 머신에서 임의의 샘플 파일을 업로드합니다.
8. 파일 업로드가 완료되면 해당 파일이 있는 줄 끝의 점 세 개를 클릭하고 **Rename**을 클릭합니다.
9. 파일 이름을 `sample.txt`로 바꾸고 **RENAME**을 클릭합니다.

### 프로젝트 Viewer 액세스 확인하기

1. **Username 2** Cloud console 탭으로 전환합니다.
2. Cloud console에서 **Navigation menu(탐색 메뉴) > Cloud Storage > Buckets**로 이동합니다.
3. **Username 2**가 해당 버킷을 볼 수 있는지 확인합니다.

---

---

## 🧩 Task 4. 프로젝트 액세스 제거하기

이 작업에서는 **Username 2**의 프로젝트 Viewer 역할을 제거하고, 이 사용자가 프로젝트에 대한 액세스 권한을 잃었는지 확인합니다.

### Username 2의 Project Viewer 역할 제거하기

1. **Username 1** Cloud console 탭으로 전환합니다.
2. Google Cloud console에서 **Navigation menu(탐색 메뉴)**의 **IAM & admin > IAM**을 클릭합니다.
3. **Username 2**가 있는 줄에서 연필 모양의 **Edit principal** 아이콘을 클릭합니다. **Viewer(뷰어)** 역할 오른쪽의 삭제 아이콘을 클릭한 다음 **SAVE**를 클릭합니다.

   > **💡 참고:** 콘솔 화면에 사용자 선택 체크박스와 **Remove Access** 버튼이 표시되는 경우에는 **Username 2**를 선택하고 **Remove Access > CONFIRM**을 클릭해도 됩니다.

   > **💡 참고:** **Username 2**의 액세스 권한을 제거하는 것이 맞는지 확인하세요. 실수로 **Username 1**의 액세스 권한을 제거하면 이 실습을 처음부터 다시 시작해야 합니다!

4. 목록에서 해당 사용자가 사라진 것을 확인하세요. 이제 이 사용자는 액세스 권한이 없습니다.

### Username 2가 액세스 권한을 잃었는지 확인하기

1. **Username 2** Cloud console 탭으로 전환합니다.
2. Cloud console에서 **Navigation menu(탐색 메뉴)**의 **Cloud overview > Dashboard**를 클릭합니다.
3. Cloud console에서 **Navigation menu(탐색 메뉴)**의 **Cloud Storage(클라우드 스토리지) > Buckets(버킷)**를 클릭합니다. 오류가 표시될 것입니다. 표시되지 않는다면 페이지를 새로고침하세요. **Username 2**는 여전히 Google Cloud 계정을 가지고 있지만 프로젝트에 대한 액세스 권한은 없습니다.

---

---

## 🧩 Task 5. 스토리지 액세스 추가하기

이 작업에서는 **Username 2**에게 **Storage Object Viewer(스토리지 객체 뷰어)** 역할을 부여한 다음, 이 사용자가 Cloud Storage에 대해 제한된 액세스 권한을 갖는지 확인합니다.

### 스토리지 권한 추가하기

1. Qwiklabs **Connection Details(연결 세부정보)** 대화상자에서 **Username 2**의 값을 복사합니다.
2. **Username 1** Cloud console 탭으로 전환합니다.
3. Google Cloud console에서 **Navigation menu(탐색 메뉴)**의 **IAM & admin > IAM**을 클릭합니다.
4. 사용자를 추가하기 위해 **Grant Access(액세스 권한 부여)**를 클릭합니다.
5. **New principals(새 주 구성원)**에 Qwiklabs **Connection Details(연결 세부정보)** 대화상자에서 복사한 **Username 2** 값을 붙여넣습니다.
6. **Select a role(역할 선택)**에서 **Cloud Storage(클라우드 스토리지) > Storage Object Viewer**를 선택합니다.
7. **SAVE**를 클릭합니다.

   > **💡 참고:** 여기서는 프로젝트 수준에서 역할을 부여하므로 **Username 2**는 프로젝트 안의 모든 버킷에 있는 객체를 조회할 수 있습니다. 실제 환경에서 특정 버킷만 조회하게 하려면 해당 버킷의 **Permissions** 화면에서 **Storage Object Viewer(스토리지 객체 뷰어)** 역할을 부여하세요.

### Username 2가 스토리지 액세스 권한을 갖는지 확인하기

1. **Username 2** Cloud console 탭으로 전환합니다.

   > **💡 참고:** **Username 2**는 Project Viewer 역할이 없기 때문에 콘솔에서 프로젝트나 프로젝트의 리소스를 볼 수 없습니다. 하지만 이 사용자는 Cloud Storage에 대한 특정 액세스 권한을 가지고 있습니다.

2. Cloud Shell을 시작하려면 **Activate Cloud Shell(Cloud Shell 활성화)**을 클릭합니다. 메시지가 표시되면 **Continue(계속)**를 클릭합니다.
3. 앞서 생성한 버킷의 내용을 확인하려면, `[YOUR_BUCKET_NAME]`을 생성한 Cloud Storage 버킷의 고유한 이름으로 바꿔서 다음 명령어를 실행합니다.

   ```bash
   gcloud storage ls gs://[YOUR_BUCKET_NAME]
   ```

   보시다시피 **Username 2**는 Cloud Storage에 대해 제한된 액세스 권한만 가지고 있습니다.

4. **Username 2** Cloud console 탭을 닫습니다. 이후 실습은 모두 **Username 1** Cloud console 탭에서 진행합니다.
5. **Username 1** Cloud console 탭으로 전환합니다.

---

---

## 🧩 Task 6. Service Account User 설정하기

이 작업에서는 서비스 계정에 좁은 범위의 권한을 할당하고, **Service Account User(서비스 계정 사용자)** 역할을 사용하는 방법을 배웁니다.

### 서비스 계정 생성하기

1. Google Cloud console에서 **Navigation menu(탐색 메뉴)**의 **IAM & Admin(IAM 및 관리자) > Service Accounts(서비스 계정)**를 클릭합니다.
2. **+ CREATE SERVICE ACCOUNT**를 클릭합니다.
3. **Service account name**에 `read-bucket-objects`를 입력합니다.
4. **CREATE AND CONTINUE**를 클릭합니다.
5. **Select a role(역할 선택)**에서 **Cloud Storage(클라우드 스토리지) > Storage Object Viewer**를 선택합니다.
6. **CONTINUE**를 클릭합니다.
7. **DONE**을 클릭합니다.

### 서비스 계정에 사용자 추가하기

1. **read-bucket-objects** 서비스 계정을 선택합니다.
2. 서비스 계정 이름 오른쪽의 점 세 개를 클릭한 다음 **Manage permissions**를 클릭합니다.

   > **💡 참고:** 이 사용자에게 **Service Account User(서비스 계정 사용자)** 역할을 부여할 것입니다. 이 역할에는 `iam.serviceAccounts.actAs` 권한이 포함되며, 사용자가 이 서비스 계정을 VM과 같은 리소스에 연결할 수 있게 해줍니다. 서비스 계정 자체의 권한을 사용자에게 직접 부여하거나 서비스 계정 키를 발급하는 역할은 아닙니다. 이 작업은 특정 사용자, 그룹, 도메인 단위로 수행할 수 있습니다. 학습 목적으로 여기서는 Altostrat.com이라는 가상의 회사에 소속된 모든 사람에게 **Service Account User(서비스 계정 사용자)** 역할을 부여합니다. Altostrat.com은 데모와 교육을 위해 사용되는 가상의 회사입니다.

3. **GRANT ACCESS** 버튼을 클릭합니다. 다음과 같이 지정하고 나머지는 기본값으로 둡니다.

   | Property | Value (type value or select option as specified) |
   |---|---|
   | New principals | `altostrat.com` |
   | Role | Service Accounts > Service Account User |

4. **SAVE**를 클릭합니다.

### Compute Engine 액세스 권한 부여하기

이제 Altostrat 조직 전체에 **Compute Instance Admin (v1)** 역할을 부여합니다.

1. Cloud console에서 **Navigation menu(탐색 메뉴)**의 **IAM & admin > IAM**을 클릭합니다.
2. **Grant Access(액세스 권한 부여)**를 클릭합니다.
3. 다음과 같이 지정하고 나머지는 기본값으로 둡니다.

   | Property | Value (type value or select option as specified) |
   |---|---|
   | New principals | `altostrat.com` |
   | Select a role | Compute Engine > Compute Instance Admin (v1) |

4. **SAVE**를 클릭합니다.

   > **💡 참고:** 이 단계는 특정 사용자를 대상으로 수행할 활동을 미리 연습해보는 것입니다. 이 작업을 통해 사용자는 VM 인스턴스에 대한 제한된 권한을 갖게 됩니다. 이 사용자는 SSH로 VM에 연결하여 일부 관리 작업을 수행할 수 있습니다.

### Service Account User로 VM 생성하기

1. Cloud console에서 **Navigation menu(탐색 메뉴)**의 **Compute Engine(컴퓨트 엔진) > VM instances(VM 인스턴스)**를 클릭합니다.
2. **CREATE INSTANCE**를 클릭합니다.
3. 다음과 같이 지정하고 나머지는 기본값으로 둡니다.

   | Property | Value (type value or select option as specified) |
   |---|---|
   | Name | `demoiam` |
   | Region | `us-central1` (또는 지정된 리전) |
   | Zone | `us-central1-c` (또는 지정된 존) |
   | Series | E2 |
   | Machine Type | e2-micro (2 vCPU, 1 GB memory) |
   | Boot disk | Debian GNU/Linux 12 (bookworm) |
   | Service account | read-bucket-objects |
   | Access scopes | Allow full access to all Cloud APIs |

4. **Create(만들기)**를 클릭합니다.

   > **💡 참고:** `cloud-platform` 액세스 범위에 해당하는 **Allow full access to all Cloud APIs**를 선택하더라도 서비스 계정의 IAM 권한이 확대되지는 않습니다. 액세스 범위는 API 호출을 추가로 제한할 수 있으며, 실제 허용 작업은 서비스 계정에 부여한 IAM 역할로 제어하는 것이 현재 권장 방식입니다.

---

---

## 🧩 Task 7. Service Account User 역할 살펴보기

이 작업에서는 SSH로 VM에 연결하여 다음 작업들을 수행하며 VM에 연결된 서비스 계정의 액세스 권한을 테스트합니다. 이 실습의 **Username 1**은 프로젝트 소유자로서 서비스 계정을 VM에 연결하는 데 필요한 `iam.serviceAccounts.actAs` 권한을 가지고 있습니다. Cloud console에서 SSH로 VM에 접속한 뒤 별도의 사용자 자격 증명을 구성하지 않으면, VM에서 실행하는 `gcloud` 명령은 연결된 서비스 계정의 자격 증명을 사용합니다.

이제 VM에 연결된 서비스 계정에 허용된 작업과 거부되는 작업을 직접 비교합니다.

### Service Account User 사용하기

1. **demoiam**에서 **SSH(SSH 접속)**를 클릭하여 터미널을 실행하고 접속합니다.
2. 다음 명령어를 실행합니다.

   ```bash
   gcloud compute instances list
   ```

   결과 (예시 출력):

   ```yaml
   ERROR: (gcloud.compute.instances.list) Some requests did not succeed.
   - Required 'compute.instances.list' permission for 'projects/PROJECT_ID'
   ```

   무슨 일이 일어난 것일까요? 왜 그럴까요?

3. 앞서 생성한 버킷에서 `sample.txt` 파일을 복사합니다. 아래 명령어의 마지막에 있는 마침표도 명령어의 일부라는 점에 유의하세요. 이는 "현재 위치"로 복사하라는 의미입니다.

   ```bash
   gcloud storage cp gs://[YOUR_BUCKET_NAME]/sample.txt .
   ```

   결과 (예시 출력):

   ```text
   Copying gs://train-test-iam/sample.txt...
   / [1 files][   28.0 B/  28.0 B]
   Operation completed over 1 objects/28.0 B.
   ```

4. 복사한 파일의 이름을 바꾸려면 다음 명령어를 실행합니다.

   ```bash
   mv sample.txt sample2.txt
   ```

5. 이름을 바꾼 파일을 버킷으로 다시 복사하려면 다음 명령어를 실행합니다.

   ```bash
   gcloud storage cp sample2.txt gs://[YOUR_BUCKET_NAME]
   ```

   결과 (예시 출력):

   ```yaml
   AccessDeniedException: 403 Caller does not have storage.objects.create access to bucket
   train-test-iam.
   ```

   > **💡 참고:** 무슨 일이 일어난 것일까요? VM에서 별도의 사용자 자격 증명을 구성하지 않았으므로 `gcloud`는 VM에 연결된 **read-bucket-objects** 서비스 계정의 자격 증명을 사용합니다. 이 서비스 계정에는 **Storage Object Viewer(스토리지 객체 뷰어)** 역할이 있어 Cloud Storage 객체를 조회하고 다운로드할 수 있지만, 인스턴스 목록 조회에 필요한 `compute.instances.list` 권한과 객체 업로드에 필요한 `storage.objects.create` 권한은 없습니다. 따라서 파일 다운로드는 성공하지만 인스턴스 목록 조회와 객체 업로드는 거부됩니다.

6. Google Cloud console에서 **Navigation menu(탐색 메뉴)**의 **IAM & admin > IAM**을 클릭합니다.
7. 목록에서 **read-bucket-objects**가 있는 줄을 찾아 연필 아이콘을 클릭합니다.

   **read-bucket-objects**는 현재 **Storage Object Viewer(스토리지 객체 뷰어)** 역할을 가지고 있습니다. **Role(역할)**을 **Cloud Storage(클라우드 스토리지) > Storage Object Creator**로 변경합니다.

8. **Save(저장)**를 클릭합니다.

   > **💡 참고:** IAM 정책 변경 사항이 적용되는 데 시간이 걸릴 수 있습니다. 다음 업로드가 바로 실패하면 잠시 기다린 후 다시 실행하세요.

9. **demoiam**의 SSH 창으로 돌아갑니다.
10. 이름을 바꾼 파일을 버킷으로 다시 복사하려면 다음 명령어를 실행합니다.

    ```bash
    gcloud storage cp sample2.txt gs://[YOUR_BUCKET_NAME]
    ```

    이번에는 서비스 계정이 올바른 권한을 가지고 있으므로 명령이 성공합니다.

---

---

## 🧩 Task 8. Review (검토)

이 실습에서는 IAM 역할을 부여하고 회수하는 작업을 연습했습니다. 먼저 **Username 2**의 프로젝트 및 Cloud Storage 접근 권한을 변경했고, 이후에는 **Service Account User(서비스 계정 사용자)** 역할을 사용하여 사용자에게 특정 서비스 계정을 VM에 연결할 권한을 부여했습니다. 또한 VM에 연결된 서비스 계정이 자신의 IAM 역할 범위 안에서만 Google Cloud 리소스에 접근한다는 점을 확인했습니다. 실제 환경에서도 서비스 계정 키를 VM에 저장하기보다 사용자 관리 서비스 계정을 VM에 연결하고, 필요한 최소 IAM 역할만 부여하는 방식을 사용하세요.

---

## End your lab (실습 종료)

---

> [← 이전 실습](06.Working%20with%20Virtual%20Machines_KR.md) · [📚 전체 목록](../README.md) · [다음 실습 →](08.Cloud%20Storage_KR.md)
