# 10. Examining Billing data with BigQuery

> [← 이전 실습](09.Implementing%20Cloud%20SQL_KR.md) · [📚 전체 목록](../README.md) · [다음 실습 →](11.Resource%20Monitoring_KR.md)

## Overview (개요)

이 실습에서는 BigQuery를 사용하여 청구(billing) 데이터를 분석하는 방법을 배웁니다.

## Objectives (목표)

이 실습에서는 다음 작업을 수행하는 방법을 배웁니다.

- Google Cloud 콘솔에서 BigQuery를 엽니다.
- 데이터세트(dataset)를 생성합니다.
- 테이블을 생성합니다.
- 버킷에 저장된 청구 파일에서 데이터를 가져옵니다(import).
- 더 큰 데이터세트에서 복잡한 쿼리를 실행합니다.

---

---

## 🧩 Task 1. BigQuery로 데이터 가져오기

### BigQuery에 로그인하여 데이터세트 생성하기

이번 작업에서는 BigQuery를 사용하여 데이터세트를 생성합니다. 그런 다음 테이블을 생성하고, 마지막으로 Cloud Storage에서 청구 데이터를 가져옵니다.

1. Google Cloud 콘솔 상단의 프로젝트 선택기에서 실습 프로젝트 ID(`qwiklabs-gcp-`로 시작)를 선택합니다.
2. **Navigation menu(탐색 메뉴)** > **BigQuery** > **Studio**를 클릭합니다. 처음 BigQuery를 여는 경우 안내 창에서 **Done(완료)**을 클릭합니다.
3. 왼쪽의 **Explorer(탐색기)** 탭에서 실습 프로젝트 ID를 클릭합니다. Explorer 탭이 보이지 않으면 왼쪽 창을 확장합니다.
4. 프로젝트 ID 옆의 **View actions(작업 보기)** 아이콘을 클릭하고 **Create dataset(데이터세트 만들기)**을 클릭합니다. 새 데이터세트를 만들 때는 **Datasets(데이터세트)**를 먼저 선택하지 않습니다.

> **💡 참고:** [Export Cloud Billing data to BigQuery Guide](https://cloud.google.com/billing/docs/how-to/export-data-bigquery)에 설명된 대로 청구 데이터를 BigQuery로 직접 내보낼 수도 있습니다. 하지만 이 실습에서는 샘플 청구 파일이 이미 준비되어 있습니다. 이 파일은 학생 계정에서 액세스할 수 있는 Cloud Storage 버킷에 저장되어 있습니다. 이 청구 정보를 BigQuery 테이블로 가져와서 살펴보게 됩니다.

5. **Create dataset(데이터세트 만들기)** 페이지에서 다음과 같이 지정합니다.

   | Property (속성) | Value (값) |
   |---|---|
   | Dataset ID | `billing_dataset` |
   | Location type | Multi-region |
   | Multi-region location | US |

6. **Advanced options(고급 옵션)**를 펼친 뒤 **Enable table expiration(테이블 만료 사용 설정)**을 선택하고, **Default maximum table age(기본 최대 테이블 수명)**에 `1`일을 입력합니다.

   > 데이터세트 위치는 생성한 후 변경할 수 없습니다. 이 실습에서 만들 테이블과 쿼리는 모두 `US` 위치를 사용합니다.

7. **Create dataset(데이터세트 만들기)**을 클릭합니다. Explorer 창에서 실습 프로젝트를 펼치고 **Datasets(데이터세트)** > **billing_dataset**을 선택해 생성되었는지 확인합니다.

### 테이블 생성 및 가져오기

1. **Explorer(탐색기)** 창에서 실습 프로젝트 > **Datasets(데이터세트)** > **billing_dataset**을 선택한 다음, **Dataset info**에서 **Create table**을 클릭하여 새 테이블을 생성합니다.
2. **Source**에서 다음과 같이 지정하고 나머지는 기본값으로 둡니다. **Browse** 목록에서 버킷을 찾지 말고, **Select file from GCS bucket** 입력란에 버킷 이름부터 직접 입력합니다. 이 입력란에서는 `gs://` 접두사를 입력하지 않습니다.

   | Property (속성) | Value (값) |
   |---|---|
   | Create table from | Google Cloud Storage |
   | Select file from GCS bucket | `cloud-training/archinfra/BillingExport-2020-09-18.avro` |
   | File format | Avro |

3. **Destination**에서 다음과 같이 지정하고 나머지는 기본값으로 둡니다.

   | Property (속성) | Value (값) |
   |---|---|
   | Table name | `sampleinfotable` |
   | Table type | Native table |

4. **Create Table**을 클릭합니다. 작업이 완료되면 왼쪽 패널의 데이터세트 아래에 테이블이 나타납니다.

---

---

## 🧩 Task 2. 테이블 검토하기

이번 작업에서는 가져온 데이터를 살펴봅니다.

1. **sampleinfotable**을 클릭합니다.

> **💡 참고:** 이 화면에는 BigQuery가 가져온 파일에서 발견한 데이터를 기반으로 자동 생성한 스키마(schema)가 표시됩니다. 문자열(string), 정수(integer), 타임스탬프(timestamp), 부동소수점(floating) 값이 있는 것을 확인하세요.

2. **Details**를 클릭합니다. **Number of Rows**에서 행 수를 확인할 수 있습니다.
3. **Preview** 탭을 클릭합니다.

---

---

## 🧩 Task 3. 간단한 쿼리 작성하기

이번 작업에서는 청구 데이터를 필터링하는 간단한 쿼리를 작성하고 실행합니다.

쿼리에서 테이블을 참조할 때는 데이터세트 ID와 테이블 ID를 모두 지정해야 하며, 프로젝트 ID는 선택 사항입니다.

> **💡 참고:** 프로젝트 ID를 지정하지 않으면 BigQuery는 현재 프로젝트를 기본값으로 사용합니다.

필요한 모든 정보는 BigQuery 인터페이스에서 확인할 수 있습니다. 왼쪽 열에서 데이터세트 ID(billing_dataset)와 테이블 ID(sampleinfotable)를 확인할 수 있습니다.

앞서 확인했듯이 테이블 이름을 클릭하면 모든 필드 이름이 나온 **Schema**가 표시됩니다.

이제 **Cost** 필드를 기준으로 간단한 쿼리를 작성해 봅니다.

1. **Compose new query**를 클릭합니다.
2. **Query Editor**에 다음을 붙여넣습니다.

   ```sql
   SELECT * FROM `billing_dataset.sampleinfotable` WHERE Cost > 0
   ```

3. **Run**을 클릭합니다. cost가 0보다 큰 행은 몇 개였나요?

---

---

## 🧩 Task 4. SQL로 대규모 청구 데이터세트 분석하기

이번 작업에서는 BigQuery를 사용하여 415,602줄의 청구 데이터로 구성된 샘플 데이터세트를 분석합니다.

1. **Compose new query**에서 **Query Editor**에 다음을 붙여넣습니다.

   ```sql
   SELECT
     billing_account_id,
     project.id,
     project.name,
     service.description,
     currency,
     currency_conversion_rate,
     cost,
     usage.amount,
     usage.pricing_unit
   FROM
     `billing_dataset.sampleinfotable`
   ```

2. **Run**을 클릭합니다. 결과 테이블에 415,602개의 청구 데이터 레코드가 있는지 확인합니다.
3. cost가 0보다 큰(즉, 요금이 발생한) 최신 레코드 100개를 찾으려면, **Compose new query**에서 **Query Editor**에 다음을 붙여넣습니다.

   ```sql
   SELECT
     service.description,
     sku.description,
     location.country,
     cost,
     project.id,
     project.name,
     currency,
     currency_conversion_rate,
     usage.amount,
     usage.unit
   FROM
     `billing_dataset.sampleinfotable`
   WHERE
     Cost > 0
   ORDER BY usage_end_time DESC
   LIMIT 100
   ```

4. **Run**을 클릭합니다.
5. 10달러를 초과하는 모든 요금을 찾으려면, **Compose new query**에서 **Query Editor**에 다음을 붙여넣습니다.

   ```sql
   SELECT
     service.description,
     sku.description,
     location.country,
     cost,
     project.id,
     project.name,
     currency,
     currency_conversion_rate,
     usage.amount,
     usage.unit
   FROM
     `billing_dataset.sampleinfotable`
   WHERE
     cost > 10
   ```

6. **Run**을 클릭합니다.
7. 청구 데이터에서 레코드가 가장 많은 제품을 찾으려면, **Compose new query**에서 **Query Editor**에 다음을 붙여넣습니다.

   ```sql
   SELECT
     service.description,
     COUNT(*) AS billing_records
   FROM
     `billing_dataset.sampleinfotable`
   GROUP BY
     service.description
   ORDER BY billing_records DESC
   ```

8. **Run**을 클릭합니다. 어떤 제품의 청구 레코드가 가장 많았나요?
9. 1달러를 초과하는 요금 중 가장 자주 사용된 제품을 찾으려면, **Compose new query**에서 **Query Editor**에 다음을 붙여넣습니다.

   ```sql
   SELECT
     service.description,
     COUNT(*) AS billing_records
   FROM
     `billing_dataset.sampleinfotable`
   WHERE
     cost > 1
   GROUP BY
     service.description
   ORDER BY
     billing_records DESC
   ```

10. **Run**을 클릭합니다. 1달러를 초과하는 청구 레코드가 가장 많은 제품은 무엇인가요?
11. 가장 일반적으로 청구되는 사용량 단위(unit of measure)를 찾으려면, **Compose new query**에서 **Query Editor**에 다음을 붙여넣습니다.

    ```sql
    SELECT
      usage.unit,
      COUNT(*) AS billing_records
    FROM
      `billing_dataset.sampleinfotable`
    WHERE cost > 0
    GROUP BY
      usage.unit
    ORDER BY
      billing_records DESC
    ```

12. **Run**을 클릭합니다. 가장 일반적으로 청구된 사용량 단위는 무엇이었나요?
13. 합산 비용(aggregate cost)이 가장 높은 제품을 찾으려면, **Compose new query**에서 **Query Editor**에 다음을 붙여넣습니다.

    ```sql
    SELECT
      service.description,
      ROUND(SUM(cost),2) AS total_cost
    FROM
      `billing_dataset.sampleinfotable`
    GROUP BY
      service.description
    ORDER BY
      total_cost DESC
    ```

14. **Run**을 클릭합니다. 총 비용이 가장 높은 제품은 무엇인가요?

---

---

## 🧩 Task 5. Review (검토)

이 실습에서는 AVRO 파일로 생성된 청구 데이터를 BigQuery로 가져왔습니다. 해당 파일에 대해 간단한 쿼리를 실행했습니다. 그런 다음 415,602개의 청구 정보 레코드가 담긴 데이터세트에 액세스했습니다. 이 데이터에 다양한 쿼리를 실행하면서, BigQuery로 쿼리를 실행하여 질문을 던지고 답을 얻는 방법을 살펴보았습니다.

---

## End your lab (실습 종료)

---

> [← 이전 실습](09.Implementing%20Cloud%20SQL_KR.md) · [📚 전체 목록](../README.md) · [다음 실습 →](11.Resource%20Monitoring_KR.md)
