# 04. Implement Private Google Access and Cloud NAT

> [← 이전 실습](03.VPC%20Networking_KR.md) · [📚 전체 목록](../README.md) · [다음 실습 →](05.Creating%20Virtual%20Machines_KR.md)

## Overview (개요)

이 실습에서는 외부 IP 주소가 없는 VM 인스턴스에 Private Google Access와 Cloud NAT를 구현합니다. 그런 다음 Google API 및 서비스의 공개 IP 주소에 대한 액세스와, 인터넷으로의 다른 연결이 정상적으로 이루어지는지 확인합니다.

외부 IP 주소가 없는 VM 인스턴스는 외부 네트워크로부터 격리됩니다. Cloud NAT를 사용하면 이런 인스턴스도 업데이트와 패치, 경우에 따라서는 부트스트래핑(bootstrapping)을 위해 인터넷에 액세스할 수 있습니다. Cloud NAT는 관리형 서비스이기 때문에 사용자의 별도 관리나 개입 없이도 고가용성(high availability)을 제공합니다.

## Objectives (목표)

이 실습에서는 다음 작업을 수행하는 방법을 배웁니다.

- 외부 IP 주소가 없는 VM 인스턴스를 구성합니다.
- Identity-Aware Proxy(IAP) 터널을 사용하여 VM 인스턴스에 연결합니다.
- 서브넷에서 Private Google Access를 활성화합니다.
- Cloud NAT 게이트웨이를 구성합니다.
- Google API 및 서비스의 공개 IP 주소와 그 외 인터넷 연결에 대한 액세스를 확인합니다.

---

---

## 🧩 Task 1. VM 인스턴스 생성하기

방화벽 규칙 몇 개와 외부 IP 주소가 없는 VM 인스턴스를 가진 VPC 네트워크를 생성하고, IAP 터널을 사용해 해당 인스턴스에 연결합니다.

### VPC 네트워크와 방화벽 규칙 생성하기

먼저 VM 인스턴스를 위한 VPC 네트워크와, SSH 액세스를 허용하는 방화벽 규칙을 생성합니다.

1. Google Cloud console에서 **Navigation menu(탐색 메뉴)**의 **VPC network(VPC 네트워크) > VPC networks(VPC 네트워크)**를 클릭합니다.
2. **Create VPC network(VPC 네트워크 만들기)**를 클릭합니다.
3. **Name(이름)**에 `privatenet`을 입력합니다.
4. **Subnet creation mode(서브넷 생성 모드)**에서 **Custom(커스텀)**을 클릭합니다.
5. **New subnet(새 서브넷)**에서 다음과 같이 지정하고 나머지는 기본값으로 둡니다.

   | Property (속성) | Value (값) |
   |---|---|
   | Name | `privatenet-us` |
   | Region | `us-central1` |
   | IPv4 address range | `10.130.0.0/20` |

   > **💡 참고:** 아직 **Private Google access(비공개 Google 액세스)**를 활성화하지 마세요!

6. **Done(완료)**을 클릭합니다.
7. **Create(만들기)**를 클릭하고 네트워크가 생성될 때까지 기다립니다.
8. 왼쪽 패널에서 **Firewall(방화벽)**을 클릭합니다.
9. **Create Firewall Rule(방화벽 규칙 만들기)**을 클릭합니다.
10. 다음과 같이 지정하고 나머지는 기본값으로 둡니다.

    | Property (속성) | Value (값) |
    |---|---|
    | Name | `privatenet-allow-ssh` |
    | Network | privatenet |
    | Targets | All instances in the network |
    | Source filter | IPv4 ranges |
    | Source IPv4 ranges | `35.235.240.0/20` |
    | Protocols and ports | Specified protocols and ports |

11. **tcp** 체크박스를 선택하고 포트 **22**를 지정합니다.
12. **Create(만들기)**를 클릭합니다.

> **💡 참고:** 프라이빗 인스턴스에 SSH로 연결하려면 방화벽에서 적절한 포트를 열어야 합니다. [IAP connections](https://cloud.google.com/iap/docs/using-tcp-forwarding)는 특정 IP 주소 범위(`35.235.240.0/20`)에서만 발생하므로, 규칙을 이 CIDR 범위로 제한할 수 있습니다.

### 공개 IP 주소가 없는 VM 인스턴스 생성하기

1. Google Cloud console에서 **Navigation menu(탐색 메뉴)**의 **Compute Engine(컴퓨트 엔진) > VM instances(VM 인스턴스)**를 클릭합니다.
2. **Create Instance(인스턴스 만들기)**를 클릭합니다.
3. **Machine configuration(머신 구성)** 페이지에서 다음과 같이 지정하고 나머지는 기본값으로 둡니다.

   | Property (속성) | Value (값) |
   |---|---|
   | Name | `vm-internal` |
   | Region | `us-central1` |
   | Zone | `us-central1-c` |
   | Series | E2 |
   | Machine type | e2-medium (2vCPU, 1 core, 4 GB memory) |

4. **OS and storage(OS 및 스토리지)**를 클릭합니다.
5. **Image(이미지)**가 **Debian GNU/Linux 12 (bookworm)**이 아니라면, **Change(변경)**를 클릭하여 **Debian GNU/Linux 12 (bookworm)**을 선택한 다음 **Select(선택)**를 클릭합니다.
6. **Networking(네트워킹)**을 클릭합니다.
7. **Network interfaces(네트워크 인터페이스)**에서 네트워크 인터페이스를 편집하여 다음과 같이 지정합니다.

   | Property (속성) | Value (값) |
   |---|---|
   | Network | privatenet |
   | Subnetwork | privatenet-us |
   | External IPv4 address | None |

   > **💡 참고:** VM 인스턴스의 기본 설정은 임시(ephemeral) 외부 IP 주소를 갖도록 되어 있습니다. 이 동작은 조직 또는 프로젝트 수준의 정책 제약 조건(policy constraint)으로 변경할 수 있습니다. VM 인스턴스의 외부 IP 주소 제어에 대해 더 알고 싶다면 [external IP address documentation](https://cloud.google.com/compute/docs/ip-addresses/reserve-static-external-ip-address#disableexternalip)을 참고하세요.

8. **Done(완료)**을 클릭합니다.
9. **Create(만들기)**를 클릭하고 VM 인스턴스가 생성될 때까지 기다립니다.
10. **VM instances(VM 인스턴스)** 페이지에서 **vm-internal**의 **External IP(외부 IP)**가 **None**인지 확인합니다.

### IAP 터널을 테스트하기 위해 vm-internal에 SSH 접속하기

1. Cloud console에서 **Activate Cloud Shell(Cloud Shell 활성화)**을 클릭합니다.
2. 메시지가 표시되면 **Continue(계속)**를 클릭합니다.
3. **vm-internal**에 연결하려면 다음 명령어를 실행합니다.

   ```bash
   gcloud compute ssh vm-internal --zone=us-central1-c --tunnel-through-iap
   ```

4. 메시지가 표시되면 **Authorize(승인)**를 클릭합니다.
5. 계속할지 묻는 메시지가 표시되면 `Y`를 입력합니다.
6. 패스프레이즈(passphrase)를 입력하라는 메시지가 표시되면 **ENTER**를 누릅니다.
7. 동일한 패스프레이즈를 다시 입력하라는 메시지가 표시되면 **ENTER**를 누릅니다.
8. **vm-internal**의 외부 연결성을 테스트하려면 다음 명령어를 실행합니다.

   ```bash
   ping -c 2 www.google.com
   ```

   **vm-internal**에는 외부 IP 주소가 없기 때문에 이 명령은 동작하지 않아야 합니다!

9. `ping` 명령어가 끝날 때까지 기다립니다.
10. Cloud Shell 인스턴스로 돌아가려면 다음 명령어를 실행합니다.

    ```bash
    exit
    ```

> **💡 참고:** 외부 IP 주소가 없는 인스턴스는 관리형 VPN 게이트웨이나 Cloud IAP 터널을 통해서만 네트워크상의 다른 인스턴스에서 접근할 수 있습니다. Cloud IAP를 사용하면 베스천 호스트(bastion host) 없이도 SSH와 RDP를 통해 상황 인식(context-aware) 방식으로 VM에 접근할 수 있습니다. 이에 대해 더 알고 싶다면 블로그 게시물 [Cloud IAP enables context-aware access to VMs via SSH and RDP without bastion hosts](https://cloud.google.com/blog/products/identity-security/cloud-iap-enables-context-aware-access-to-vms-via-ssh-and-rdp-without-bastion-hosts)를 참고하세요.

---

---

## 🧩 Task 2. Private Google Access 활성화하기

외부 IP 주소가 없는 VM 인스턴스는 Private Google Access를 사용하여 Google API 및 서비스의 외부 IP 주소에 접근할 수 있습니다. VPC 네트워크에서 Private Google Access는 기본적으로 비활성화되어 있습니다.

### Cloud Storage 버킷 생성하기

Google API 및 서비스에 대한 액세스를 테스트하기 위한 Cloud Storage 버킷을 생성합니다.

1. Google Cloud console에서 **Navigation menu(탐색 메뉴)**의 **Cloud Storage(클라우드 스토리지) > Buckets(버킷)**를 클릭합니다.
2. **Create(만들기)**를 클릭합니다.
3. 다음과 같이 지정하고 나머지는 기본값으로 둡니다.

   | Property (속성) | Value (값) |
   |---|---|
   | Name | 전역적으로 고유한 이름 입력 |
   | Location type | Multi-region |

4. **Create(만들기)**를 클릭합니다. public access prevention을 활성화할지 묻는 메시지가 표시되면 체크되어 있는지 확인하고 **Confirm(확인)**을 클릭합니다. 스토리지 버킷 이름을 기록해 둡니다.
5. 버킷 이름을 환경 변수에 저장합니다.

   ```bash
   export MY_BUCKET=[여기에 버킷 이름 입력]
   ```

6. `echo`로 값을 확인합니다.

   ```bash
   echo $MY_BUCKET
   ```

### 이미지 파일을 버킷에 복사하기

공개 Cloud Storage 버킷에 있는 이미지를 자신의 버킷으로 복사합니다.

1. Cloud Shell에서 다음 명령어를 실행합니다.

   ```bash
   gcloud storage cp gs://cloud-training/gcpnet/private/access.svg gs://$MY_BUCKET
   ```

2. Cloud console에서 버킷 이름을 클릭하여 이미지가 복사되었는지 확인합니다.

Cloud console에서 이미지 이름을 클릭하면 Private Google Access가 어떻게 구현되는지 예시를 확인할 수 있습니다.

### VM 인스턴스에서 이미지에 액세스하기

1. Cloud Shell에서 버킷의 이미지를 복사해보려면 다음 명령어를 실행합니다.

   ```bash
   gcloud storage cp gs://$MY_BUCKET/*.svg .
   ```

   Cloud Shell에는 외부 IP 주소가 있기 때문에 이 명령은 정상적으로 동작해야 합니다!

2. **vm-internal**에 연결하려면 다음 명령어를 실행합니다.

   ```bash
   gcloud compute ssh vm-internal --zone=us-central1-c --tunnel-through-iap
   ```

3. 메시지가 표시되면 `Y`를 입력해 계속 진행합니다.
4. 버킷 이름을 환경 변수에 저장합니다.

   ```bash
   export MY_BUCKET=[여기에 버킷 이름 입력]
   ```

5. `echo`로 값을 확인합니다.

   ```bash
   echo $MY_BUCKET
   ```

6. **vm-internal**에서 이미지를 복사해보려면 다음 명령어를 실행합니다.

   ```bash
   gcloud storage cp gs://$MY_BUCKET/*.svg .
   ```

   Private Google Access가 (기본값으로) 비활성화되어 있어서 **vm-internal**은 VPC 네트워크 내부로만 트래픽을 보낼 수 있기 때문에, 이 명령은 동작하지 않아야 합니다.

7. 요청을 중단하려면 **Ctrl+Z**를 누릅니다.

### Private Google Access 활성화하기

Private Google Access는 서브넷 수준에서 활성화됩니다. 활성화하면, 프라이빗 IP 주소만 가진 서브넷 내 인스턴스도 기본 라우트(0.0.0.0/0)를 통해 기본 인터넷 게이트웨이로 트래픽을 보내는 방식으로 Google API 및 서비스에 접근할 수 있습니다.

1. Cloud console에서 **Navigation menu(탐색 메뉴)**의 **VPC network(VPC 네트워크) > VPC networks(VPC 네트워크)**를 클릭합니다.
2. **privatenet**을 클릭하여 네트워크를 엽니다.
3. **Subnets**를 클릭한 다음 **privatenet-us**를 클릭합니다.
4. **Edit(수정)**를 클릭합니다.
5. **Private Google access(비공개 Google 액세스)**에서 **On**을 선택합니다.
6. **Save(저장)**를 클릭합니다.

   > **💡 참고:** Private Google Access를 활성화하는 것은 서브넷에서 **On**을 선택하는 것만큼이나 간단합니다!

7. **vm-internal**의 Cloud Shell에서 이미지를 **vm-internal**로 복사해보기 위해 다음 명령어를 실행합니다.

   ```bash
   gcloud storage cp gs://$MY_BUCKET/*.svg .
   ```

   **vm-internal**의 서브넷에서 **Private Google Access**가 활성화되어 있기 때문에 이 명령은 정상적으로 동작해야 합니다!

8. Cloud Shell 인스턴스로 돌아가려면 다음 명령어를 실행합니다.

   ```bash
   exit
   ```

9. Cloud Shell 인스턴스로 돌아가기 위해 필요하다면 다시 한번 `exit`를 입력합니다.

   ```bash
   exit
   ```

> **💡 참고:** Private Google Access와 함께 사용할 수 있는 API 및 서비스 목록을 확인하려면, [Private access options for services Guide](https://cloud.google.com/vpc/docs/private-access-options#pga-supported)의 지원 서비스 항목을 참고하세요.

---

---

## 🧩 Task 3. Cloud NAT 게이트웨이 구성하기

이제 **vm-internal**은 외부 IP 주소 없이도 특정 Google API 및 서비스에 접근할 수 있지만, 여전히 업데이트나 패치를 위해 인터넷에 접근할 수는 없습니다. **vm-internal**이 인터넷에 접근할 수 있도록 Cloud NAT 게이트웨이를 구성합니다.

### VM 인스턴스 업데이트 시도하기

1. Cloud Shell에서 패키지 인덱스를 다시 동기화해보려면 다음 명령어를 실행합니다.

   ```bash
   sudo apt-get update
   ```

   출력은 다음과 같이 끝나야 합니다(예시 출력).

   ```text
   ... Reading package lists... Done
   ```

   Cloud Shell에는 외부 IP 주소가 있기 때문에 이 명령은 정상적으로 동작해야 합니다!

2. **vm-internal**에 연결하려면 다음 명령어를 실행합니다.

   ```bash
   gcloud compute ssh vm-internal --zone=us-central1-c --tunnel-through-iap
   ```

3. 메시지가 표시되면 `Y`를 입력해 계속 진행합니다.
4. **vm-internal**의 패키지 인덱스를 다시 동기화해보려면 다음 명령어를 실행합니다.

   ```bash
   sudo apt-get update
   ```

   **vm-internal**은 Google API 및 서비스에만 접근할 수 있기 때문에, 이 명령은 Google Cloud 패키지에 대해서만 동작해야 합니다!

5. 요청을 중단하려면 **Ctrl+C**를 누릅니다.

### Cloud NAT 게이트웨이 구성하기

Cloud NAT는 리전(regional) 리소스입니다. 리전 내 모든 서브넷의 모든 범위에서 오는 트래픽을 허용하도록 구성할 수도 있고, 리전 내 특정 서브넷에서만, 또는 특정 기본·보조 CIDR 범위에서만 오는 트래픽을 허용하도록 구성할 수도 있습니다.

1. Google Cloud console 제목 표시줄의 **Search(검색)** 필드에 `Network services`를 입력한 다음, **Products & Page** 섹션에서 **Network services**를 클릭합니다.
2. **Network service** 페이지에서 **Network services** 옆의 **Pin(고정)**을 클릭합니다.
3. **Cloud NAT**를 클릭합니다.
4. NAT 게이트웨이를 구성하기 위해 **Get started(시작하기)**를 클릭합니다.
5. 다음과 같이 지정합니다.

   | Property (속성) | Value (값) |
   |---|---|
   | Gateway name | `nat-config` |
   | Network | privatenet |
   | Region | `us-central1` |

6. **Cloud Router(Cloud 라우터)**에서 **Create new router(새 라우터 만들기)**를 선택합니다.
7. **Name(이름)**에 `nat-router`를 입력합니다.
8. **Create(만들기)**를 클릭합니다.
9. **Create(만들기)**를 클릭합니다.
10. 게이트웨이의 상태가 **Running**으로 바뀔 때까지 기다립니다.

> **💡 참고:** **NAT mapping** 섹션에서는 NAT 게이트웨이에 매핑할 서브넷을 선택할 수 있습니다. NAT를 수행할 때 사용할 정적 IP 주소를 수동으로 할당할 수도 있습니다. 이 실습에서는 NAT 매핑 구성을 변경하지 마세요.

### Cloud NAT 게이트웨이 확인하기

NAT 구성이 VM에 전파되기까지 최대 3분 정도 걸릴 수 있으므로, 다시 인터넷에 접근을 시도하기 전에 최소 1분 정도 기다리세요.

1. **vm-internal**의 Cloud Shell에서, **vm-internal**의 패키지 인덱스를 다시 동기화해보려면 다음 명령어를 실행합니다.

   ```bash
   sudo apt-get update
   ```

   출력은 다음과 같이 끝나야 합니다(예시 출력).

   ```text
   ... Reading package lists... Done
   ```

   **vm-internal**이 NAT 게이트웨이를 사용하고 있기 때문에 이 명령은 정상적으로 동작해야 합니다!

2. Cloud Shell 인스턴스로 돌아가려면 다음 명령어를 실행합니다.

   ```bash
   exit
   ```

> **💡 참고:** Cloud NAT 게이트웨이는 아웃바운드(outbound) NAT만 구현하며 인바운드(inbound) NAT는 구현하지 않습니다. 다시 말해, VPC 네트워크 외부의 호스트는 인스턴스가 시작한 연결에만 응답할 수 있으며, NAT를 통해 인스턴스로 새로운 연결을 직접 시작할 수는 없습니다.

---

---

## 🧩 Task 4. Cloud NAT Logging 구성 및 로그 확인하기

[Cloud NAT logging](https://cloud.google.com/nat/docs/monitoring)을 사용하면 NAT 연결 및 오류를 로깅할 수 있습니다. Cloud NAT logging이 활성화되면, 다음 두 가지 시나리오마다 로그 항목이 하나씩 생성될 수 있습니다.

- NAT를 사용하는 네트워크 연결이 생성될 때
- NAT를 위한 포트를 사용할 수 없어서 패킷이 드롭될 때

두 종류의 이벤트를 모두 로깅하도록 선택할 수도 있고, 둘 중 하나만 로깅하도록 선택할 수도 있습니다. 생성된 로그는 Cloud Logging으로 전송됩니다.

### 로깅 활성화하기

로깅이 활성화되면, 수집된 모든 로그는 기본적으로 Cloud Logging으로 전송됩니다. 특정 로그만 전송되도록 필터링할 수도 있습니다.

NAT 게이트웨이를 생성할 때 이 값들을 지정할 수도 있고, 생성 후에 편집하여 지정할 수도 있습니다. 다음 안내에서는 기존 NAT 게이트웨이에 대해 로깅을 활성화하는 방법을 보여줍니다.

1. Google Cloud console에서 **Navigation menu(탐색 메뉴)**의 **Network services(네트워크 서비스) > Cloud NAT**를 클릭합니다.
2. `nat-config` 게이트웨이를 클릭한 다음 **Edit(수정)**를 클릭합니다.
3. **Advanced configurations** 드롭다운을 클릭하여 해당 섹션을 엽니다.
4. **Logging(로깅)**에서 **Translation and errors**를 선택한 다음 **Save(저장)**를 클릭합니다.

### Cloud Logging에서 NAT 로깅 확인하기

이제 `nat-config` 게이트웨이에 대한 Cloud NAT logging을 설정했으니, 로그를 어디서 볼 수 있는지 확인해봅니다.

1. `nat-config`를 클릭하여 세부정보를 펼칩니다. 그런 다음 **View in Logs Explorer**를 클릭합니다.
2. 그러면 **Logs Explorer**가 새 탭에서 열립니다.

이 시점에는 아직 로그가 없는 것을 확인할 수 있는데, 방금 이 게이트웨이에 대해 이 기능을 활성화했기 때문입니다.

> **💡 참고:** 이 탭을 열어둔 채로 다른 Google Cloud console 탭으로 돌아가세요.

### 로그 생성하기

다시 한번 정리하면, Cloud NAT 로그는 다음 시퀀스에 대해 생성됩니다.

- NAT를 사용하는 네트워크 연결이 생성될 때
- NAT를 위한 포트를 사용할 수 없어서 패킷이 드롭될 때

로그가 생성되는지 확인하기 위해 다시 한번 내부 VM에 호스트를 연결해봅니다.

1. **vm-internal**의 Cloud Shell에서, **vm-internal**의 패키지 인덱스를 다시 동기화해보려면 다음 명령어를 실행합니다.

   ```bash
   gcloud compute ssh vm-internal --zone=us-central1-c --tunnel-through-iap
   ```

2. 메시지가 표시되면 `Y`를 입력해 계속 진행합니다.
3. 다음 명령어를 실행하여 **vm-internal**의 패키지 인덱스를 다시 동기화해봅니다.

   ```bash
   sudo apt-get update
   ```

   출력은 다음과 같이 나타나야 합니다(예시 출력).

   ```text
   ... Reading package lists... Done
   ```

4. Cloud Shell 인스턴스로 돌아가려면 다음 명령어를 실행합니다.

   ```bash
   exit
   ```

이 연결을 열었을 때 로그에 새로운 내용이 나타나는지 확인해봅니다.

### 로그 확인하기

- **Logs Explorer** 탭으로 돌아가서, 내비게이션 메뉴에서 **Logs Explorer**를 클릭합니다.

내부 VM에 연결한 후 생성된 새로운 로그 2개가 표시되는 것을 확인할 수 있습니다.

> **💡 참고:** 몇 분 정도 기다려야 할 수도 있습니다. 그래도 로그가 보이지 않는다면, **로그 생성하기** 섹션의 1~4단계를 반복한 다음 로깅 페이지를 새로고침하세요.

이렇게 로그를 통해 우리가 연결한 VPC 네트워크와 사용한 연결 방식에 대한 세부 정보를 확인할 수 있습니다. 여러 라벨과 세부정보를 자유롭게 펼쳐서 살펴보세요.

---

---

## 🧩 Task 5. Review (검토)

외부 IP 주소가 없는 인스턴스인 **vm-internal**을 생성하고, IAP 터널을 사용해 안전하게 연결했습니다. 그런 다음 Private Google Access를 활성화하고 NAT 게이트웨이를 구성하여, **vm-internal**이 Google API 및 서비스와 다른 공개 IP 주소에 접근할 수 있는지 확인했습니다.

외부 IP 주소가 없는 VM 인스턴스는 외부 네트워크로부터 격리됩니다. Cloud NAT를 사용하면 이런 인스턴스도 업데이트와 패치, 경우에 따라서는 부트스트래핑을 위해 인터넷에 접근할 수 있습니다. Cloud NAT는 관리형 서비스이기 때문에 사용자의 별도 관리나 개입 없이도 고가용성을 제공합니다.

IAP는 VM 인스턴스에 연결할 때 기존 프로젝트 역할과 권한을 그대로 사용합니다. 기본적으로 인스턴스 소유자만 **IAP Secured Tunnel User** 역할을 가지고 있습니다.

IAP 터널링을 사용하여 다른 사용자에게 VM에 대한 액세스 권한을 부여하는 방법은 Cloud Architecture Center의 [Grant access to additional users Guide](https://cloud.google.com/solutions/building-internet-connectivity-for-private-vms#grant_access_to_additional_users)를 참고하세요.

---

## End your lab (실습 종료)

---

> [← 이전 실습](03.VPC%20Networking_KR.md) · [📚 전체 목록](../README.md) · [다음 실습 →](05.Creating%20Virtual%20Machines_KR.md)
