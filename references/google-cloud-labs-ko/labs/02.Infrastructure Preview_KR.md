# 02. Infrastructure Preview

> [← 이전 실습](01.Working%20with%20the%20Google%20Cloud%20Console%20and%20Cloud%20Shell_KR.md) · [📚 전체 목록](../README.md) · [다음 실습 →](03.VPC%20Networking_KR.md)

## Overview (개요)

이 실습에서는 **Marketplace(마켓플레이스)**를 사용하여 몇 분 만에 정교한 배포를 구축합니다. 이 실습은 여러 Google Cloud 인프라 서비스가 실제로 동작하는 모습을 보여주며 플랫폼의 강력함을 보여줍니다. 이 과정 후반부에서 자세히 다룰 기술들을 미리 소개합니다.

## Objectives (목표)

이 실습에서는 다음 작업을 수행하는 방법을 배웁니다.

- Marketplace를 사용하여 Jenkins Continuous Integration 환경을 구축합니다.
- Jenkins UI에서 서비스를 관리할 수 있는지 확인합니다.
- SSH를 통해 Virtual Machine 호스트에서 서비스를 관리합니다.

---

---

## 🧩 Task 1. Marketplace를 사용하여 배포 구축하기

이 작업에서는 Google Cloud Marketplace를 사용하여 사전 구성된 Jenkins 인스턴스를 배포합니다. Marketplace 인터페이스를 살펴보고, 무료(Open Source) Jenkins 패키지를 선택한 다음 배포를 실행합니다. 서비스 계정, 존(zone), 머신 유형 등 기본적인 배포 설정을 구성합니다.

### Marketplace로 이동하기

1. Google Cloud console에서 **Navigation menu(탐색 메뉴)**의 **Marketplace(마켓플레이스)**를 클릭합니다.
2. 검색창에 **Jenkins**를 검색하여 무료 오픈소스 Jenkins 배포 항목(예: Click to Deploy 또는 Free 패키지)을 찾습니다.
3. 배포 항목을 클릭하여 해당 소프트웨어가 제공하는 서비스에 대한 설명을 읽어봅니다. Jenkins는 오픈소스 continuous integration(지속적 통합) 환경입니다. Jenkins에서는 정해진 일정에 따라 소프트웨어를 빌드하거나 데이터를 백업하는 등의 작업을 수행하는 job(작업)을 정의할 수 있습니다. 설명 왼쪽에 Jenkins의 일부로 설치되는 소프트웨어 목록이 표시되는 것을 확인하세요.

지금 사용하는 서비스인 Marketplace는 Google Cloud의 일부입니다. 사전 구성된 템플릿을 통해 인프라와 소프트웨어가 자동으로 프로비저닝됩니다.

### Jenkins 실행하기

1. **Get Started(시작하기)** (또는 **Launch / Deploy(실행 / 배포)**)를 클릭합니다.
2. 배포 내용을 확인하고, 약관에 동의한 다음 **Agree(동의)**를 클릭합니다.
3. `Successfully agreed to terms` 팝업이 뜨면 **Deploy(배포)**를 클릭합니다.
4. 메시지가 표시되면 Compute Engine API와 Infrastructure Manager API에 대해 **Enable(사용 설정)**을 클릭합니다.
5. Deployment 페이지에서 **Deployment Service Account** 아래 **Existing Account**를 선택하고, **Compute Engine default service account**를 선택합니다.
6. Zone은 `us-central1-c` (또는 실습에 할당된 존)으로 선택합니다.
7. **Machine type(머신 유형)**에서 **Series(시리즈)**는 **E2**를, **Machine type(머신 유형)**은 **e2-standard-2 (2 vCPU, 1 core, 8GB memory)**를 선택합니다.
8. 나머지 항목은 모두 기본값으로 두고 **Deploy(배포)**를 클릭합니다.

> **💡 참고:** Jenkins가 배포된 후 표시되는 경고는 무시해도 됩니다.

> **💡 참고:** 배포 설정 및 VM 확보에 1~2분 정도 걸립니다. 작업이 진행되는 동안 상태를 지켜볼 수 있습니다. 배포가 완료되면 인스턴스 접속 정보 및 URL이 표시됩니다.

---

---

## 🧩 Task 2. 배포 살펴보기

이 작업에서는 배포된 Jenkins 인스턴스를 살펴봅니다. 관리자 자격 증명을 가져오고, Jenkins 웹 인터페이스에 로그인하여 기능을 익혀봅니다.

### 설치된 소프트웨어 확인 및 Jenkins 로그인하기

1. **Details** 탭에서 **Admin user**와 **Admin password (Temporary)** 값을 확인하고 텍스트 편집기에 기록해 둡니다. (비밀번호 확인 방법이 콘솔 창에 안내되어 있는 경우 해당 안내를 따릅니다)
2. **Site URL** 옆의 링크를 클릭하여 다른 브라우저 탭에서 사이트를 확인합니다. 오류가 발생하면 페이지를 몇 차례 새로고침해야 할 수도 있습니다.
3. 메시지가 표시되면 **Continue to site**를 클릭합니다.

> **💡 참고:** http 404 오류가 발생하면 사이트 주소에서 `/jenkins` 부분을 제거하고 Enter를 누르세요. 예를 들면 다음과 같습니다: `http://35.238.162.236`

4. **Admin user**와 **Admin password** 값을 사용해 로그인합니다.
5. 로그인 후 Jenkins를 Customize하라는 메시지가 표시되면 **Install suggested plugins**를 클릭한 다음, 설치가 끝나면 **Restart(다시 시작)**를 클릭합니다. 재시작에는 몇 분 정도 걸립니다.

> **💡 참고:** 설치 오류가 발생하면 설치를 **retry**(재시도)하고, 그래도 다시 실패하면 오류를 무시하고 넘어가서 재시작하기 전에 **save and finish**(저장 후 완료)를 진행하세요.

### Jenkins 살펴보기

1. Jenkins 인터페이스 왼쪽 패널에서 **Manage Jenkins**를 클릭합니다. 사용 가능한 모든 작업을 살펴보세요. 이제 Jenkins를 관리할 준비가 되었습니다. 이 실습의 초점은 Jenkins 관리가 아니라 Google Cloud 인프라이므로, 이 메뉴가 존재한다는 것을 확인하는 것이 이 단계의 목적입니다.
2. Jenkins 서비스가 열린 브라우저 창을 그대로 둡니다. 다음 작업에서 사용합니다.

> **💡 참고:** 이제 소프트웨어가 정상적으로 설치되어 동작하는 것을 확인했습니다. 다음 작업에서는 서비스가 호스팅되는 VM에 SSH 터미널 세션을 열어서, 해당 서비스에 대한 관리자 권한을 가지고 있는지 확인합니다.

---

---

## 🧩 Task 3. 서비스 관리하기

이 작업에서는 SSH를 통해 Jenkins VM에 접속하여 Jenkins 서비스를 관리합니다. 명령줄 도구를 사용하여 서비스를 중지하고 재시작함으로써, 배포된 애플리케이션에 대한 관리자 권한을 확인합니다.

### 배포 확인 및 VM에 SSH 접속하기

1. **Navigation menu(탐색 메뉴)**에서 **Compute Engine(컴퓨트 엔진) > VM instances(VM 인스턴스)**로 이동합니다.
2. 생성된 Jenkins VM(예: **jenkins-1-vm**)을 클릭합니다.
3. **SSH(SSH 접속)**를 클릭하여 Jenkins 서버에 접속합니다.

### 서비스 종료 및 재시작하기

1. SSH 창에서 다음 명령어를 입력하여 실행 중인 Jenkins 서비스를 종료합니다.

   - **일반 Linux/Systemd 패키지인 경우:**
     ```bash
     sudo systemctl stop jenkins
     ```
   - **(참고) Bitnami 기반 패키지인 경우:**
     ```bash
     sudo /opt/bitnami/ctlscript.sh stop
     ```

2. Jenkins UI가 열려 있는 브라우저 창을 새로고침합니다. 서비스가 중지되었기 때문에 더 이상 Jenkins 인터페이스가 표시되지 않습니다.
3. SSH 창에서 다음 명령어를 입력하여 서비스를 재시작합니다.

   - **일반 Linux/Systemd 패키지인 경우:**
     ```bash
     sudo systemctl restart jenkins
     # 또는
     sudo systemctl start jenkins
     ```
   - **(참고) Bitnami 기반 패키지인 경우:**
     ```bash
     sudo /opt/bitnami/ctlscript.sh restart
     ```

4. Jenkins UI 브라우저 창으로 돌아가서 새로고침합니다. 서비스에 다시 접속할 수 있게 될 때까지 몇 차례 새로고침해야 할 수도 있습니다.
5. SSH 창에서 `exit`를 입력하여 SSH 터미널 세션을 종료합니다.

---

---

## 🧩 Task 4. Review (검토)

몇 분 만에 완전한 Continuous Integration 솔루션을 실행할 수 있었습니다. Jenkins UI를 통한 사용자 액세스가 가능함을 확인했고, 서비스가 호스팅된 VM에 SSH로 접속하여 서비스를 중지했다가 다시 시작함으로써 Jenkins에 대한 관리자 권한을 가지고 있음을 확인했습니다.

---

## End your lab (실습 종료)

---

> [← 이전 실습](01.Working%20with%20the%20Google%20Cloud%20Console%20and%20Cloud%20Shell_KR.md) · [📚 전체 목록](../README.md) · [다음 실습 →](03.VPC%20Networking_KR.md)
