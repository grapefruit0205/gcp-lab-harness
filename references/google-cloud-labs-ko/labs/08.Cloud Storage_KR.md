# 08. Cloud Storage

> [← 이전 실습](07.Exploring%20IAM_KR.md) · [📚 전체 목록](../README.md) · [다음 실습 →](09.Implementing%20Cloud%20SQL_KR.md)

## Overview (개요)

Cloud Storage는 Google Cloud의 핵심 리소스로, 다양한 고급 기능을 제공합니다. 이 실습에서는 설계에 유용하게 활용할 수 있는 여러 Cloud Storage 기능들을 실습해봅니다. Cloud Storage는 Google Cloud console, 권장 명령줄 도구인 `gcloud storage`, 또는 다양한 프로그래밍 언어용 Cloud Storage 클라이언트 라이브러리를 통해 사용할 수 있습니다.

## Objectives (목표)

이 실습에서는 다음 작업을 수행하는 방법을 배웁니다.

- 버킷을 생성하고 사용합니다.
- 액세스를 제한하기 위한 접근 제어 목록(access control list)을 설정합니다.
- 자체 암호화 키를 사용합니다.
- 객체 버전 관리(Object Versioning)를 구현합니다.
- 디렉터리 동기화를 사용합니다.

---

---

## 🧩 Task 1. 준비하기

이 작업에서는 Cloud Storage 버킷을 생성합니다. 그런 다음 작업에서 사용할 샘플 파일을 다운로드합니다.

### Cloud Storage 버킷 생성하기

1. Google Cloud console에서 **Navigation menu(탐색 메뉴)**의 **Cloud Storage(클라우드 스토리지) > Buckets(버킷)**를 클릭합니다.

   > **💡 참고:** 버킷 이름은 전역적으로 고유해야 합니다. 고유한 이름을 만들기 위해 `PROJECT_ID_1`의 일부를 이름에 사용할 수 있습니다. 예를 들어 `PROJECT_ID_1`이 `myproj-154920`이라면 버킷 이름은 `storecore154920`처럼 지을 수 있습니다.

2. **Create(만들기)**를 클릭합니다.
3. 다음과 같이 지정하고 나머지는 기본값으로 둡니다.

   | Property | Value (type value or select option as specified) |
   |---|---|
   | Name | 전역적으로 고유한 이름을 입력합니다 |
   | Location type | Region |
   | Region | `us-central1` (또는 지정된 리전) |
   | Enforce public access prevention on this bucket | unchecked |
   | Access control | Fine-grained (*object-level permission in addition to your bucket-level permissions*) |

4. 버킷 이름을 기록해 두세요. 이후 실습에서 사용되며 `[BUCKET_NAME_1]`로 참조됩니다.
5. **Create(만들기)**를 클릭합니다.

   > **💡 참고:** 이 실습은 객체 ACL을 공개로 변경하므로 Public Access Prevention을 사용하지 않습니다. 하지만 프로젝트나 조직에서 `storage.publicAccessPrevention` 정책을 강제하면 버킷 설정을 해제해도 공개 ACL을 추가할 수 없습니다.

### CURL로 샘플 파일을 다운로드하고 사본 2개 만들기

1. Cloud console에서 **Activate Cloud Shell(Cloud Shell 활성화)**을 클릭합니다.
2. 메시지가 표시되면 **Continue(계속)**를 클릭합니다.
3. `[BUCKET_NAME_1]`을 환경 변수에 저장합니다.

   ```bash
   export BUCKET_NAME_1=<enter bucket name 1 here>
   ```

4. `echo`로 확인합니다.

   ```bash
   echo $BUCKET_NAME_1
   ```

5. 다음 명령어를 실행하여 샘플 파일을 다운로드합니다 (이 샘플 파일은 공개적으로 제공되는 Hadoop 문서 HTML 파일입니다).

   ```bash
   curl https://hadoop.apache.org/docs/current/\
   hadoop-project-dist/hadoop-common/\
   ClusterSetup.html > setup.html
   ```

6. 파일 사본을 만들려면 다음 명령어를 실행합니다.

   ```bash
   cp setup.html setup2.html
   cp setup.html setup3.html
   ```

---

---

## 🧩 Task 2. 접근 제어 목록 (ACLs)

이 작업에서는 Cloud Storage 버킷으로 복사하는 파일에 대한 액세스를 제한하기 위해 접근 제어 목록(ACL)을 설정합니다.

### 버킷으로 파일을 복사하고 접근 제어 목록 구성하기

1. 첫 번째 파일을 버킷으로 복사하려면 다음 명령어를 실행합니다.

   ```bash
   gcloud storage cp setup.html gs://$BUCKET_NAME_1/
   ```

2. `setup.html`에 할당된 기본 접근 목록을 확인하려면 다음 명령어를 실행합니다.

   ```bash
   gcloud storage objects describe gs://$BUCKET_NAME_1/setup.html \
     --format="multi(acl:format=json)" > acl.txt
   cat acl.txt
   ```

3. 접근 목록을 비공개(private)로 설정하고 결과를 확인하려면 다음 명령어를 실행합니다.

   ```bash
   gcloud storage objects update gs://$BUCKET_NAME_1/setup.html \
     --predefined-acl=private
   gcloud storage objects describe gs://$BUCKET_NAME_1/setup.html \
     --format="multi(acl:format=json)" > acl2.txt
   cat acl2.txt
   ```

   결과 (예상 출력):

   ```json
   []
   ```

   > **💡 참고:** 빈 배열은 이 객체에 ACL 방식으로 명시적으로 부여된 사용자가 없다는 의미입니다. 프로젝트 또는 버킷 수준에서 IAM 역할을 받은 사용자의 권한까지 제거되었다는 의미는 아닙니다.

4. 파일을 공개적으로 읽을 수 있도록 접근 목록을 업데이트하려면 다음 명령어를 실행합니다.

   ```bash
   gcloud storage objects update gs://$BUCKET_NAME_1/setup.html \
     --add-acl-grant=entity=allUsers,role=READER
   gcloud storage objects describe gs://$BUCKET_NAME_1/setup.html \
     --format="multi(acl:format=json)" > acl3.txt
   cat acl3.txt
   ```

   결과 (예상 출력):

   ```json
   [
     {
       "entity": "allUsers",
       "role": "READER"
     }
   ]
   ```

   > **💡 참고:** `allUsers`는 인증 여부와 관계없이 인터넷상의 모든 사용자를 의미합니다. 따라서 이 ACL이 적용되면 객체 URL을 아는 사용자는 누구나 `setup.html`을 읽을 수 있습니다.

   > **💡 참고:** `412 Precondition Failed` 오류가 발생한다면 프로젝트 또는 상위 조직에서 Public Access Prevention을 강제하고 있는 것입니다. 해당 정책이 적용된 환경에서는 이 공개 ACL 단계를 수행할 수 없습니다.

### Cloud Console에서 파일 확인하기

1. Google Cloud console에서 **Navigation menu(탐색 메뉴)**의 **Cloud Storage(클라우드 스토리지) > Buckets(버킷)**를 클릭합니다.
2. **[BUCKET_NAME_1]**을 클릭합니다.
3. `setup.html` 파일의 **Public access(공개 액세스)**에 **Public link(공개 링크)**가 표시되는지 확인합니다.

### 로컬 파일을 삭제하고 Cloud Storage에서 다시 복사하기

1. **Cloud Shell**로 돌아갑니다. 필요하다면 **Activate Cloud Shell(Cloud Shell 활성화)**을 클릭합니다.
2. setup 파일을 삭제하려면 다음 명령어를 실행합니다.

   ```bash
   rm setup.html
   ```

3. 파일이 삭제되었는지 확인하려면 다음 명령어를 실행합니다.

   ```bash
   ls
   ```

4. 버킷에서 파일을 다시 복사하려면 다음 명령어를 실행합니다.

   ```bash
   gcloud storage cp gs://$BUCKET_NAME_1/setup.html setup.html
   ```

---

---

## 🧩 Task 3. 고객 제공 암호화 키 (CSEK)

이 작업에서는 Cloud Storage 버킷에 저장된 파일을 암호화하기 위한 CSEK 키를 생성합니다.

### CSEK 키 생성하기

다음 단계에서는 표준 Base64로 인코딩된 256비트 AES 키가 필요합니다.

1. 키를 생성하고 `CSEK_1` 환경 변수에 저장하려면 다음 명령어를 실행합니다.

   ```bash
   export CSEK_1=$(python3 -c \
     'import base64, os; print(base64.b64encode(os.urandom(32)).decode())')
   echo "$CSEK_1"
   ```

   결과 (예시 출력):

   ```text
   tmxElCaabWvJqR7uXEWQF39DhWTcDvChzuCmpHe6sb0=
   ```

   > **⚠️ 주의:** 예시 키를 사용하지 마세요. CSEK는 Google이 보관하지 않으므로 키를 잃으면 해당 객체를 복호화할 수 없습니다. 실제 운영 환경에서는 키를 터미널 기록이나 일반 텍스트 파일에 남기지 말고 승인된 보안 저장소에 백업하세요.

### gcloud storage 키 저장소 구성하기

`gcloud storage`는 YAML 형식의 키 저장소에서 기본 암호화 키와 복호화 키 목록을 읽을 수 있습니다.

1. 키 저장소 파일을 엽니다.

   ```bash
   nano "$HOME/csek-keys.yaml"
   ```

2. 다음 내용을 붙여넣고 `YOUR_CSEK_1`을 `echo "$CSEK_1"`에서 확인한 실제 키로 바꿉니다.

   ```yaml
   encryption_key: YOUR_CSEK_1
   decryption_keys: []
   ```

3. 파일을 저장하려면 **Ctrl+O**, **ENTER**를 누르고, nano를 종료하려면 **Ctrl+X**를 누릅니다.
4. 현재 사용자만 키 파일을 읽을 수 있도록 권한을 제한하고 `gcloud storage`에 등록합니다.

   ```bash
   chmod 600 "$HOME/csek-keys.yaml"
   gcloud config set storage/key_store_path "$HOME/csek-keys.yaml"
   gcloud config get storage/key_store_path
   ```

### 나머지 setup 파일 업로드(암호화)하고 Cloud Console에서 확인하기

1. 나머지 setup.html 파일들을 업로드하려면 다음 명령어를 실행합니다.

   ```bash
   gcloud storage cp setup2.html gs://$BUCKET_NAME_1/
   gcloud storage cp setup3.html gs://$BUCKET_NAME_1/
   ```

2. Cloud console로 돌아갑니다.
3. **[BUCKET_NAME_1]**을 클릭합니다. `setup2.html`과 `setup3.html` 파일 모두 고객 암호화(customer-encrypted)된 것으로 표시됩니다.

### 로컬 파일 삭제 후 새 파일을 복사하고 암호화 확인하기

1. Cloud Shell에서 로컬 파일을 삭제하려면 다음 명령어를 실행합니다.

   ```bash
   rm setup.html setup2.html setup3.html
   ```

2. 버킷에서 파일을 다시 복사하려면 다음 명령어를 실행합니다.

   ```bash
   gcloud storage cp gs://$BUCKET_NAME_1/setup.html ./
   gcloud storage cp gs://$BUCKET_NAME_1/setup2.html ./
   gcloud storage cp gs://$BUCKET_NAME_1/setup3.html ./
   ```

3. 암호화된 파일들이 제대로 복원되었는지 확인하려면 다음 명령어를 실행합니다.

   ```bash
   cat setup.html
   cat setup2.html
   cat setup3.html
   ```

---

---

## 🧩 Task 4. CSEK 키 순환(rotate)하기

이 작업에서는 지속적인 데이터 보안을 위해 Cloud Storage 데이터 암호화에 사용되는 CSEK를 순환(rotate)합니다.

### 새 CSEK 키 생성하고 키 저장소 갱신하기

1. 새 키를 생성하려면 다음 명령어를 실행합니다.

   ```bash
   export CSEK_2=$(python3 -c \
     'import base64, os; print(base64.b64encode(os.urandom(32)).decode())')
   echo "$CSEK_2"
   ```

2. 키 저장소 파일을 엽니다.

   ```bash
   nano "$HOME/csek-keys.yaml"
   ```

3. 새 키를 기본 암호화 키로 지정하고 기존 키를 복호화 키 목록에 넣습니다. `YOUR_CSEK_2`와 `YOUR_CSEK_1`을 각각 실제 키로 바꿉니다.

   ```yaml
   encryption_key: YOUR_CSEK_2
   decryption_keys:
   - YOUR_CSEK_1
   ```

4. 파일을 저장하려면 **Ctrl+O**, **ENTER**를 누르고, nano를 종료하려면 **Ctrl+X**를 누릅니다.

### 파일 1의 키를 다시 쓰고 기존 복호화 키를 잠시 제거하기

파일이 CSEK로 암호화되어 있을 때 새 암호화 키와 기존 복호화 키를 지정하여 객체를 다시 쓰면(rewrite), 기존 키로 복호화한 다음 새 키로 다시 암호화합니다.

여기서는 `setup2.html`의 키만 다시 쓰고 `setup3.html`은 그대로 두어, 키를 제대로 순환하지 않았을 때 어떤 일이 벌어지는지 확인합니다.

1. 다음 명령어를 실행합니다.

   ```bash
   gcloud storage objects update gs://$BUCKET_NAME_1/setup2.html \
     --encryption-key="$CSEK_2" --decryption-keys="$CSEK_1"
   ```

2. 키 저장소 파일을 엽니다.

   ```bash
   nano "$HOME/csek-keys.yaml"
   ```

3. 기존 키를 복호화 키 목록에서 잠시 제거합니다.

   ```yaml
   encryption_key: YOUR_CSEK_2
   decryption_keys: []
   ```

4. 파일을 저장하려면 **Ctrl+O**, **ENTER**를 누르고, nano를 종료하려면 **Ctrl+X**를 누릅니다.

### setup2와 setup3 다운로드하기

1. `setup2.html`을 다운로드하려면 다음 명령어를 실행합니다.

   ```bash
   gcloud storage cp gs://$BUCKET_NAME_1/setup2.html recover2.html
   ```

2. `setup3.html`을 다운로드하려면 다음 명령어를 실행합니다.

   ```bash
   gcloud storage cp gs://$BUCKET_NAME_1/setup3.html recover3.html
   ```

   > **💡 참고:** `setup2.html`은 새 키로 다시 암호화했기 때문에 다운로드됩니다. `setup3.html`은 아직 기존 키로 암호화되어 있고 현재 키 저장소에서 기존 키를 제거했으므로 다운로드가 실패합니다. 기존 키 자체를 잃은 것은 아니기 때문에 다음 단계에서 복구할 수 있습니다.

### setup3의 키도 순환하고 구성 정리하기

1. 기존 키로 `setup3.html`을 복호화하고 새 키로 다시 암호화합니다.

   ```bash
   gcloud storage objects update gs://$BUCKET_NAME_1/setup3.html \
     --encryption-key="$CSEK_2" --decryption-keys="$CSEK_1"
   ```

2. 새 키로 `setup3.html`을 다운로드할 수 있는지 확인합니다.

   ```bash
   gcloud storage cp gs://$BUCKET_NAME_1/setup3.html recover3.html
   ```

3. Soft Delete로 보존될 수 있는 이전 세대를 복호화할 수 있도록 기존 키를 키 저장소의 복호화 키 목록에 다시 추가합니다.

   ```bash
   nano "$HOME/csek-keys.yaml"
   ```

   ```yaml
   encryption_key: YOUR_CSEK_2
   decryption_keys:
   - YOUR_CSEK_1
   ```

   파일을 저장하려면 **Ctrl+O**, **ENTER**를 누르고, nano를 종료하려면 **Ctrl+X**를 누릅니다.

4. 이후 작업에서 새 파일이 의도치 않게 CSEK로 암호화되지 않도록 현재 `gcloud` 구성에서 키 저장소 연결을 해제하고 환경 변수를 정리합니다.

   ```bash
   gcloud config unset storage/key_store_path
   unset CSEK_1 CSEK_2
   ```

   이렇게 두 객체의 활성 버전에 대한 CSEK 키 순환을 완료했습니다. `$HOME/csek-keys.yaml`에 남아 있는 두 키는 안전하게 보관하세요. Soft Delete나 Object Versioning으로 보존된 이전 세대가 기존 키를 사용할 수 있으므로, 실제 운영 환경에서는 보존 기간이 끝나고 모든 세대의 재암호화를 확인하기 전까지 기존 키를 파기하지 마세요.

---

---

## 🧩 Task 5. 라이프사이클 관리 사용 설정하기

이 작업에서는 지정된 기간이 지난 후 객체 삭제를 자동화하기 위해 Cloud Storage 버킷에 라이프사이클 관리를 사용 설정합니다.

### 버킷의 현재 라이프사이클 정책 확인하기

- 현재 라이프사이클 정책을 확인하려면 다음 명령어를 실행합니다.

  ```bash
  gcloud storage buckets describe gs://$BUCKET_NAME_1 \
    --format="default(lifecycle_config)"
  ```

  > **💡 참고:** 라이프사이클 구성이 없습니다. 다음 단계에서 하나를 생성합니다.

### JSON 라이프사이클 정책 파일 생성하기

1. `life.json`이라는 파일을 생성하려면 다음 명령어를 실행합니다.

   ```bash
   nano life.json
   ```

2. `life.json` 파일에 다음 값을 붙여넣습니다.

   ```json
   {
     "rule":
     [
       {
         "action": {"type": "Delete"},
         "condition": {"age": 31}
       }
     ]
   }
   ```

   > **💡 참고:** 이 설정은 Cloud Storage에게 31일이 지난 후 객체를 삭제하도록 지시합니다. 버킷에 Soft Delete가 설정되어 있다면 객체는 즉시 영구 삭제되지 않고 Soft Delete 보존 기간 동안 복구 가능한 상태로 유지됩니다.

3. 파일을 저장하려면 **Ctrl+O**, **ENTER**를 누르고, nano를 종료하려면 **Ctrl+X**를 누릅니다.

### 정책 설정 및 확인하기

1. 정책을 설정하려면 다음 명령어를 실행합니다.

   ```bash
   gcloud storage buckets update gs://$BUCKET_NAME_1 \
     --lifecycle-file=life.json
   ```

2. 정책을 확인하려면 다음 명령어를 실행합니다.

   ```bash
   gcloud storage buckets describe gs://$BUCKET_NAME_1 \
     --format="default(lifecycle_config)"
   ```

---

---

## 🧩 Task 6. 버전 관리 사용 설정하기

이 작업에서는 객체의 변경 이력을 보존하고 특정 세대를 복원하기 위해 Cloud Storage 버킷에 Object Versioning을 사용 설정합니다.

> **💡 참고:** 새 버킷에는 기본적으로 7일 Soft Delete가 적용됩니다. Google Cloud는 단순한 우발적·악의적 삭제 보호에는 Soft Delete를 우선 권장합니다. Object Versioning은 여러 객체 세대를 직접 조회하고 복원해야 할 때 사용하며, 버킷 자체가 삭제되는 상황은 보호하지 않습니다.

### 버킷의 버전 관리 상태를 확인하고 버전 관리 사용 설정하기

1. 버킷의 현재 버전 관리 상태를 확인하려면 다음 명령어를 실행합니다.

   ```bash
   gcloud storage buckets describe gs://$BUCKET_NAME_1 \
     --format="default(versioning_enabled)"
   ```

   > **💡 참고:** 결과가 `null`이면 Object Versioning이 사용 설정되어 있지 않은 것입니다.

2. 버전 관리를 사용 설정하려면 다음 명령어를 실행합니다.

   ```bash
   gcloud storage buckets update gs://$BUCKET_NAME_1 --versioning
   ```

3. 버전 관리가 사용 설정되었는지 확인하려면 다음 명령어를 실행합니다.

   ```bash
   gcloud storage buckets describe gs://$BUCKET_NAME_1 \
     --format="default(versioning_enabled)"
   ```

4. 설정 변경이 적용될 때까지 최소 30초 기다립니다. 그런 다음 파일을 수정하기 전에 현재 원본 객체의 generation을 저장합니다.

   ```bash
   export ORIGINAL_GENERATION=$(gcloud storage objects describe \
     gs://$BUCKET_NAME_1/setup.html --format="value(generation)")
   export VERSION_NAME="gs://$BUCKET_NAME_1/setup.html#$ORIGINAL_GENERATION"
   echo "$VERSION_NAME"
   ```

### 버킷에서 샘플 파일의 여러 버전 만들기

1. 샘플 파일의 크기를 확인합니다.

   ```bash
   ls -al setup.html
   ```

2. `setup.html` 파일을 엽니다.

   ```bash
   nano setup.html
   ```

3. 파일 크기를 바꾸기 위해 `setup.html`에서 아무 줄이나 5줄을 삭제합니다.
4. 파일을 저장하려면 **Ctrl+O**, **ENTER**를 누르고, nano를 종료하려면 **Ctrl+X**를 누릅니다.
5. 파일을 버킷으로 복사합니다. `-v`는 생성된 객체의 generation이 포함된 URL을 출력하는 `--print-created-message` 옵션입니다. 버전 보존은 앞서 사용 설정한 Object Versioning이 수행합니다.

   ```bash
   gcloud storage cp -v setup.html gs://$BUCKET_NAME_1
   ```

6. `setup.html` 파일을 엽니다.

   ```bash
   nano setup.html
   ```

7. 파일 크기를 바꾸기 위해 `setup.html`에서 또 다른 5줄을 삭제합니다.
8. 파일을 저장하려면 **Ctrl+O**, **ENTER**를 누르고, nano를 종료하려면 **Ctrl+X**를 누릅니다.
9. 파일을 다시 버킷으로 복사하여 새로운 객체 세대를 만듭니다.

   ```bash
   gcloud storage cp -v setup.html gs://$BUCKET_NAME_1
   ```

### 파일의 모든 버전 나열하기

1. 파일의 모든 버전을 나열하려면 다음 명령어를 실행합니다.

   ```bash
   gcloud storage ls --all-versions --long \
     gs://$BUCKET_NAME_1/setup.html
   ```

2. 각 객체 URL 뒤의 `#숫자`가 generation입니다. 출력 순서에 의존하지 않고 앞에서 저장한 원본 객체 URL을 사용합니다. `VERSION_NAME`을 다시 확인하세요.

   ```bash
   echo "$VERSION_NAME"
   ```

   결과 (예시 출력):

   ```text
   gs://BUCKET_NAME_1/setup.html#1584457872853517
   ```

### 가장 오래된 원본 버전의 파일을 다운로드하고 복구 확인하기

1. 원본 버전의 파일을 다운로드합니다.

   ```bash
   gcloud storage cp "$VERSION_NAME" recovered.txt
   ```

2. 복구를 확인하려면 다음 명령어를 실행합니다.

   ```bash
   ls -al setup.html
   ls -al recovered.txt
   ```

   > **💡 참고:** 백업 버전에서 원본 파일을 복구했습니다. 줄을 삭제했기 때문에 원본 파일이 현재 버전보다 크다는 점을 확인하세요.

---

---

## 🧩 Task 7. 디렉터리를 버킷과 동기화하기

이 작업에서는 `gcloud storage rsync` 명령어를 사용하여 Cloud Shell의 로컬 디렉터리와 그 하위 디렉터리를 Cloud Storage 버킷과 동기화합니다.

### 중첩 디렉터리를 만들고 버킷과 동기화하기

버킷으로 재귀적으로 복사될 때 어떤 일이 일어나는지 확인하기 위해 중첩된 디렉터리 구조를 만듭니다.

1. 다음 명령어를 실행합니다.

   ```bash
   mkdir firstlevel
   mkdir ./firstlevel/secondlevel
   cp setup.html firstlevel
   cp setup.html firstlevel/secondlevel
   ```

2. Cloud Shell의 `firstlevel` 디렉터리를 버킷과 동기화하려면 다음 명령어를 실행합니다.

   ```bash
   gcloud storage rsync ./firstlevel \
     gs://$BUCKET_NAME_1/firstlevel --recursive
   ```

### 결과 확인하기

1. Google Cloud console에서 **Navigation menu(탐색 메뉴)**의 **Cloud Storage(클라우드 스토리지) > Buckets(버킷)**를 클릭합니다.
2. **[BUCKET_NAME_1]**을 클릭합니다. 버킷 안의 하위 폴더들을 확인하세요.
3. **/firstlevel**을 클릭한 다음 **/secondlevel**을 클릭합니다.
4. Cloud console에서 확인한 내용을 다음 명령어의 결과와 비교해 보세요.

   ```bash
   gcloud storage ls -r gs://$BUCKET_NAME_1/firstlevel
   ```

5. Cloud Shell을 종료합니다.

   ```bash
   exit
   ```

---

---

## 🧩 Task 8. Review (검토)

이 실습에서는 버킷과 객체를 생성하고 다루는 방법을 배웠으며, Cloud Storage의 다음 기능들에 대해 배웠습니다.

- CSEK: 고객 제공 암호화 키(Customer-supplied encryption key)
  - 자체 암호화 키를 사용합니다.
  - 키를 순환(rotate)합니다.
- ACL: 접근 제어 목록(Access control list)
  - ACL을 비공개(private)로 설정하고 공개(public)로 수정합니다.
- 라이프사이클 관리(Lifecycle management)
  - 31일이 지난 후 객체에 Delete 작업을 수행하도록 정책을 설정합니다.
- Soft Delete와 객체 버전 관리(Object Versioning)
  - 기본 Soft Delete와 Object Versioning의 목적 차이를 이해합니다.
  - 여러 객체 세대를 생성하고 저장해 둔 generation으로 원본을 복원합니다.
- 디렉터리 동기화(Directory synchronization)
  - Cloud Shell 디렉터리를 버킷과 재귀적으로 동기화합니다.

---

## End your lab (실습 종료)

---

> [← 이전 실습](07.Exploring%20IAM_KR.md) · [📚 전체 목록](../README.md) · [다음 실습 →](09.Implementing%20Cloud%20SQL_KR.md)
