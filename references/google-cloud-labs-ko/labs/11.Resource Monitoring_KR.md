# 11. Resource Monitoring

> [← 이전 실습](10.Examining%20Billing%20data%20with%20BigQuery_KR.md) · [📚 전체 목록](../README.md) · [다음 실습 →](12.Configuring%20Google%20Cloud%20HA%20VPN_KR.md)

## Overview (개요)

이 실습에서는 Cloud Monitoring을 사용하여 Google Cloud에서 실행되는 애플리케이션에 대한 인사이트를 얻는 방법을 배웁니다.

## Objectives (목표)

이 실습에서는 다음 작업을 수행하는 방법을 배웁니다.

- 차트가 포함된 커스텀 대시보드를 생성하여 Cloud Monitoring을 살펴봅니다.
- 여러 조건을 가진 알림(alert)을 생성합니다.
- 리소스 그룹을 생성합니다.
- 가동 시간 확인(uptime check)을 생성합니다.

---

---

## 🧩 Task 1. Cloud Monitoring 열기

이번 작업에서는 Google Cloud 리소스를 모니터링하기 위해 Cloud Monitoring을 엽니다.

### 모니터링할 리소스 확인하기

모니터링할 VM 인스턴스 3개가 이미 생성되어 있습니다.

- Google Cloud console에서 **Navigation menu(탐색 메뉴)**의 **Compute Engine(컴퓨트 엔진) > VM instances(VM 인스턴스)**를 클릭합니다. **nginxstack-1**, **nginxstack-2**, **nginxstack-3** 인스턴스를 확인하세요.

### VM이 없는 경우 CLI로 배포하기

실습 프로젝트에 VM이 사전 생성되어 있지 않은 경우, Cloud Shell에서 다음 명령어를 실행합니다.

```bash
gcloud compute instances create nginxstack-1 nginxstack-2 nginxstack-3 \
  --zone=us-central1-a \
  --machine-type=e2-micro \
  --network=default \
  --subnet=default \
  --tags=http-server \
  --image-family=debian-12 \
  --image-project=debian-cloud \
  --boot-disk-size=10GB \
  --boot-disk-type=pd-balanced \
  --metadata=startup-script='#!/bin/bash
apt-get update
apt-get install -y nginx
systemctl enable --now nginx'
```

### Cloud Monitoring 열기

Cloud Monitoring은 수동으로 워크스페이스를 생성하는 방식 대신 metrics scope를 사용합니다. 실습 프로젝트는 기본적으로 자기 자신을 모니터링하므로, 별도의 워크스페이스나 무료 체험 계정을 만들 필요가 없습니다.

1. Google Cloud 콘솔 상단의 프로젝트 선택기에서 실습 프로젝트가 선택되어 있는지 확인합니다.
2. 제목 표시줄의 **Search(검색)** 필드에 `Monitoring`을 입력한 다음, **Products & Pages(제품 및 페이지)** 섹션의 **Monitoring**을 클릭합니다.
3. Monitoring 개요 페이지가 열리고 VM 인스턴스의 측정항목을 볼 수 있으면 준비가 완료된 것입니다.

> **💡 참고:** 여러 프로젝트를 함께 모니터링해야 하는 경우에만 **Monitoring > Settings**에서 metrics scope에 프로젝트를 추가합니다. 이 실습에서는 추가 작업이 필요하지 않습니다.

---

---

## 🧩 Task 2. 커스텀 대시보드

이번 작업에서는 Cloud Monitoring에서 커스텀 대시보드를 생성하고 리소스 사용량 데이터를 살펴봅니다.

### 대시보드 생성하기

1. 왼쪽 패널에서 **Dashboards(대시보드)**를 클릭합니다.
2. **+Create Dashboard(+대시보드 만들기)**를 클릭합니다.
3. **New Dashboard Name(새 대시보드 이름)**에 `My Dashboard`를 입력합니다.

### 차트 추가하기

1. **Add Widget(위젯 추가)**을 클릭한 다음 **Line**을 선택합니다.
2. **Widget Title(위젯 제목)**에 차트 이름을 지정합니다(선택한 항목에 따라 저장 전에 수정할 수 있습니다).
3. **Metric(측정항목)** 필드 드롭다운에 `CPU utilization` 또는 `CPU usage`를 입력하고 **VM Instance > Instance**를 클릭합니다. **CPU utilization(CPU 사용률)** 또는 **CPU usage**를 선택하고 **Apply**를 클릭합니다.
4. **참고:** **CPU utilization(CPU 사용률)**을 찾을 수 없다면 **Active**의 체크를 해제하세요.
5. **+ Add Filter**를 클릭하여 다양한 옵션을 살펴봅니다.
6. **Apply**를 클릭하여 차트를 생성합니다.

### Metrics Explorer

**Metrics Explorer**를 사용하면 대시보드에 차트를 만들지 않고도 리소스와 지표(metric)를 살펴볼 수 있습니다. 방금 만든 차트를 **Metrics Explorer**로 다시 만들어 보세요.

1. 왼쪽 패널에서 **Metrics explorer**를 클릭합니다.
2. 드롭다운에서 지표를 선택합니다.
3. 다양한 옵션을 살펴보면서 앞서 만든 차트를 다시 만들어 봅니다.

> **💡 참고:** Metrics Explorer에서는 아직 모든 지표를 사용할 수 있는 것은 아니므로, 이전 단계에서 사용한 것과 정확히 같은 지표를 찾지 못할 수도 있습니다.

---

---

## 🧩 Task 3. 알림 정책(Alerting policies)

이번 작업에서는 CPU 사용량이 지정한 임곗값을 초과할 때 알려주는 알림 정책을 Cloud Monitoring에서 생성합니다.

### 알림 생성 및 첫 번째 조건 추가하기

1. 왼쪽 패널에서 **Alerting**을 선택합니다.
2. **+ Create Policy**를 클릭합니다.
3. **Select a metric** 드롭다운을 클릭합니다. **Active** 옵션의 체크를 해제합니다.
4. 리소스 및 지표 이름 필터 입력란에 `VM Instance`를 입력하고 **VM Instance > Instance**를 클릭합니다. **CPU usage** 또는 **CPU Utilization**을 선택하고 **Apply**를 클릭합니다. **참고:** **VM Instance** 리소스 유형을 찾을 수 없다면 페이지를 새로고침해야 할 수도 있습니다.

5. **Rolling windows**를 **1 min**으로 설정합니다.
6. **Next(다음)**를 클릭합니다. **Threshold position**을 **Above Threshold**로 설정하고 **Threshold value**를 `20`으로 설정합니다.
7. **Next(다음)**를 클릭합니다.

### 두 번째 조건 추가하기

1. **+ADD ALERT CONDITION**을 클릭합니다.
2. 위 단계를 반복하여 이 정책의 두 번째 조건을 지정합니다. 예를 들어 다른 인스턴스에 대해 동일한 조건을 반복합니다. **Next(다음)**를 클릭합니다.
3. **Multi-condition trigger**에서 **All conditions are met**을 선택합니다.
4. **Next(다음)**를 클릭합니다.

### 알림 설정 구성 및 알림 정책 완료하기

1. **Notification Channels** 옆의 드롭다운 화살표를 클릭한 다음 **Manage Notification Channels**를 클릭합니다.

   **Notification channels** 페이지가 새 탭에서 열립니다.

2. 페이지를 아래로 스크롤하여 **Email**의 **ADD NEW**를 클릭합니다.
3. **Email Address** 필드에 본인의 이메일 주소를 입력하고 **Display name**을 입력합니다.
4. **Save(저장)**를 클릭합니다.
5. 이전의 **Configure notifications and finalize alert** 탭으로 돌아갑니다.
6. **Notification Channels**를 다시 클릭한 다음 **Refresh(새로고침)** 아이콘을 클릭하여 앞서 입력한 표시 이름을 불러옵니다. 필요하면 **Notification Channels**를 다시 클릭합니다.
7. 이제 본인의 **Display name**을 선택하고 **OK**를 클릭합니다.
8. **Alert policy name** 필드에 원하는 이름을 입력합니다.
9. **Next(다음)**를 클릭합니다.
10. 알림을 검토하고 **Create Policy**를 클릭합니다.

---

---

## 🧩 Task 4. 리소스 그룹

이번 작업에서는 리소스를 정리하기 위해 Cloud Monitoring에서 그룹을 생성합니다.

1. 왼쪽 패널에서 **Groups**를 클릭합니다.
2. **+ Create Group**을 클릭합니다.
3. 그룹 이름을 입력합니다. 예: `VM instances`
4. **Criteria** 섹션의 **Contains** 아래 **Value** 필드에 `nginx`를 입력합니다.
5. **DONE**을 클릭합니다.
6. **CREATE**를 클릭합니다.
7. Cloud Monitoring이 그룹을 위해 생성한 대시보드를 검토합니다.

---

---

## 🧩 Task 5. 가동 시간 모니터링(Uptime monitoring)

이번 작업에서는 리소스 가용성을 모니터링하기 위한 가동 시간 확인(uptime check)을 Cloud Monitoring에서 생성합니다.

1. **Monitoring** 탭에서 **Uptime Checks**를 클릭합니다.
2. **+ Create Uptime Check**를 클릭합니다.
3. 다음과 같이 지정하고 나머지는 기본값으로 둡니다.

   | Property (속성) | Value (값) |
   |---|---|
   | Protocol | HTTP |
   | Resource Type | Instance |
   | Applies To | Group |
   | Group | *본인의 그룹 선택* |
   | Check Frequency | 1 minute |

4. **Continue(계속)**를 클릭하여 나머지 세부정보는 기본값으로 둡니다. **Alert & Notification** 탭에서 드롭다운으로부터 **Notification Channels**를 선택합니다.
5. **Continue(계속)**를 클릭합니다.
6. **Title**에 가동 시간 확인을 위한 이름을 원하는 대로 입력합니다.
7. **Test**를 클릭하여 가동 시간 확인이 리소스에 연결될 수 있는지 확인합니다.
8. 녹색 체크 표시가 보이면 모두 정상적으로 연결된 것입니다. **Create(만들기)**를 클릭합니다.

구성한 가동 시간 확인이 활성화되기까지는 시간이 조금 걸립니다.

---

---

## 🧩 Task 6. 알림 비활성화하기

이번 작업에서는 Cloud Monitoring에서 기존 알림 정책을 비활성화합니다.

알림 정책은 프로젝트가 삭제된 후에도 재설치가 필요할 경우를 대비해 한동안 활성 상태로 유지됩니다. 이 실습은 종료 후 프로젝트에 다시 액세스할 수 없으므로, 앞서 생성한 알림 정책을 제거합니다.

1. **Alerting** 섹션으로 이동합니다.
2. **view all Alert policies**를 클릭하고, **Policies** 세부정보 페이지 상단의 **Enabled** 토글을 클릭합니다.
3. 알림 정책을 끌지 확인하는 메시지가 표시되면 **Turn Off**를 클릭합니다.

---

---

## 🧩 Task 7. Review (검토)

이 실습에서는 다음 내용을 배웠습니다.

- 프로젝트를 모니터링합니다.
- Cloud Monitoring 워크스페이스를 생성합니다.
- 여러 조건을 가진 알림을 생성합니다.
- 대시보드에 차트를 추가합니다.
- 리소스 그룹을 생성합니다.
- 서비스에 대한 가동 시간 확인을 생성합니다.

---

## End your lab (실습 종료)

---

> [← 이전 실습](10.Examining%20Billing%20data%20with%20BigQuery_KR.md) · [📚 전체 목록](../README.md) · [다음 실습 →](12.Configuring%20Google%20Cloud%20HA%20VPN_KR.md)
