# Checkpoint — 이전 하네스 실습 삭제 완료 — 2026-08-26 14:43

## The story so far

D-028 “백업 하지말고 전부 destroy 해줘.”에 따라 하네스 소유 이전 실습을 정리했다. Phase 6 저장 삭제 plan은 10 destroyed로 완료됐고 Minecraft VM·월드/부팅 disk·IP·firewall·network/subnet·backup bucket·SA/IAM이 활성 목록에서 사라졌다. 새 백업/스냅샷은 만들지 않았다.

실제 VM·disk·address·router 각각 0, 하네스 활성 잔여 0, 모든 기록된 Terraform state가 빈 상태다. 오래된 Phase 5 stale state도 정리했고 미적용 Phase 4·7·foundation 계획은 생성하지 않고 종료했다. 미적용 Task는 skipped로 남겼으며 Phase 7/8 Cloud E2E 완료로 주장하지 않는다.

공통 project/ADC/billing preflight, 원래 project IAM·활성 API·default network 및 비대상 리소스 전후 동일성을 확인했다. 증거는 ignored `artifacts/cleanup-before-phase08.PSOwfC/result.json`이다. 기존 Minecraft bucket은 soft-deleted이며 API hardDeleteTime은 **2026-09-02 14:40:59 KST**다. 활성 삭제와 복구 보존을 구분한다.

## Decided

- D-028: 이전 Minecraft 보존·Phase 07 새 apply 대기를 이번 무백업 정리로 대체했다. 저장 delete-only plan/hash와 실제 inventory로 범위를 검증했다.
- 기존 soft-delete 보존 정책은 변경/우회하지 않았으며 삭제 bucket을 복구하지 않았다.
- Terraform 밖 5개 리소스는 소유 범위를 확인할 때까지 보존한다(Q-008).
- 이번에는 cleanup 기록만 로컬 변경했다. Git commit/push나 Phase 8 apply는 수행하지 않았다.

## Waiting on the user

Q-008: `junseok-lab` bucket, `mynet-us-vm-us-central1-a-20260825000711-ynklv6qi` snapshot, `lampstack`·`read-bucket-objects` SA, `privatenet-allow-ssh` firewall도 이전 실습 삭제 대상인가? 비차단 질문을 보냈으며 아직 확인되지 않았다. 원래 default network/공통 SA는 별도 보존 대상이다.

## Next first action

`cd /home/grapefruit/gcp-lab-harness && jq . artifacts/cleanup-before-phase08.PSOwfC/result.json`로 정리 결과와 남겨둔 5개를 읽고, 사용자가 추가 삭제를 명시하면 해당 리소스 identity·사용처를 다시 조회해 범위를 확정한다.

## Tried

- 과거 verified manifest만으로 리소스가 살아 있다고 판단하지 않는다. Phase 5의 기록과 달리 Cloud에서는 이미 없어 state만 갱신했다.
- `gcloud storage ls --buckets --soft-deleted --json`은 HTTP404였고 빈 목록으로 처리하지 않았다. 공식 JSON API softDeleted/pagination 조회로 실제 복구 보존을 확인했다.
- 새 Phase 7 plan 승인 대기를 정리 요청으로 이어서 apply하지 않았다. 해당 run은 never-applied 종료이며 재개에는 새 계획이 필요하다.
- 프로젝트 내 기존 리소스를 모두 하네스 실습으로 가정해 지우지 않는다. 소유 불명 5개는 Q-008로 분리했다.
