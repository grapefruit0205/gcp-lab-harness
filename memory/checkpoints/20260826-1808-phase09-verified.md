# Checkpoint — Phase09 HTTP400 수정 적용·실제 검증 중 — 2026-08-26 18:04

## The story so far

Phase09 run `p09-260826-eb03`에서 DB root 권한 누락1044를 진단했고, 첫 역할+비밀번호 동시 API 요청은400이었다. BUILT_IN 명시·역할 operation 완료 후 별도 비밀번호 요청으로 보완했다. D-041 승인 bundle `e701120a9f6d8ef03a5df23bf41f8d0e056d6238cd7d7ca3dee37ce14658e707` 적용은exit0,Terraform0added/0changed/0destroyed·DB API 성공으로400이 재발하지 않았다.

실제 verifier session15423은 Proxy DB 연결/WordPress 설치를 통과했고 private 단계 진행 중이다. 로그는 `artifacts/phase-09-separated-role-cloud-verify.log`. 전체SQL/HTTP E2E는 아직 진행 중이다.70tests/TF validate/mock/gate 재검사PASS. 리소스 삭제·교체·Phase08/shared lib/원문·commit/push 변경 없음. 실행 소스는 승인 SHA 그대로 유지한다.

## Decided

- D-041: 제시된 e701… 수정 계획 적용·실제 검증 요청, Q-018 closed. 동일 승인·일반 쉘은 다시 묻지 않는다.
- D-036/D-037: 실패해도 리소스/state/로그 보존·진단한다. 자동 destroy 금지.
- 기존 저장 계정/project·Phase08 유지, 다른 Phase·Git 게시·삭제/교체 승인 없음.

## Waiting on the user

현재 승인 대기 없음. 과거 별도 run PSA 잔여(Q-012)와 shared 복구 이관(Q-014)은 이번 범위 밖으로 남아 있다.

## Next first action

session15423 및 `/home/grapefruit/gcp-lab-harness/artifacts/phase-09-separated-role-cloud-verify.log`의 완료를 확인한다. 실패해도 destroy하지 않는다.

## Tried

- API 비밀번호 초기화 성공만으로 DB 사용 가능을 판단한 경로: root USAGE뿐이라 wordpress1044. 실제 권한/SQL 검사가 필요했다.
- type 없는 역할+비밀번호 동시 요청:400/INTERNAL_ERROR. 정확한옛backend원문은없지만 새분리요청 API·Proxy 실제DB는통과했다.
- 이전 전체cleanup은 VM/SQL을 삭제했다. 현재 전용 보존 경로는 실제 실패에도 환경을 유지했다.
- 현재 state SHA `4b4cbeebaaa1c51c4a9e55126f9d6f7f91749b67cf53613a54ae0f247dc0379b`; 현재bundle apply receipt와 일치. 원시 로그/state/비밀은Git 제외다.
