# Checkpoint — Phase 07 구현·plan 완료, apply 승인 대기 — 2026-08-26

## The story so far

observed: `/home/grapefruit/gcp-lab-harness`의 Phase 07 IAM 골격을 보완했다. PGA·cloud-platform scope, 정확한 HTTP403/permission 판정, IAM 전파 대기, actAs/Compute matrix, metadata guest probe, 임시 IAM rollback과 소유권 cleanup을 구현했다. Python 회귀 23개·Terraform mock 5개·validate/fmt·Phase gate·07–15 offline suite가 통과했다. 새 run `p07-260826-72bd`의 저장 plan은 12개 생성·변경 0·삭제 0이다. 아직 apply나 실제 IAM 실습은 수행하지 않았다. Phase 06 VM은 read-only 재조회에서 같은 ID·IP·lastStartTimestamp의 RUNNING 상태였다.

## Decided

- D-017: Phase 쉘 실행은 반복 질문하지 않되 정확한 saved plan bundle SHA 승인은 유지한다.
- 사용자 요청은 Phase 07 구현과 Terraform apply다. Phase 07 commit·push·최종 destroy는 아직 승인받지 않았다.
- 승인 후보 bundle: `cbadfcca92660a44a40653665e8ad1bc35cb61895b5699eb1467b0908ddd095e`.
- Terraform 자체는 전용 VPC/subnet/IAP firewall, test SA 3개, runner TokenCreator 3개, private bucket/fixture와 workload Viewer를 생성한다. IAM 실습 verify는 별도 action plan에 묶인 임시 project Viewer/Instance Admin·SA actAs·bucket role 전이와 private e2-micro/10GB VM 생성을 수행한다. 실습 종료 시 임시 grant를 회수하며 VM은 cleanup까지 남아 비용이 발생한다.

## Waiting on the user

위 bundle SHA에 대한 apply 및 IAM 실습 검증 승인. 아직 승인 파일을 만들거나 Cloud 변경을 실행하지 않았다. 승인 전에는 apply하지 않는다.

## Next first action

`sha256sum /home/grapefruit/gcp-lab-harness/artifacts/runs/p07-260826-72bd/phase-07/plan-bundle.json`으로 승인 후보 SHA를 다시 확인한다.

## Tried

- 첫 후보 `p07-260826-c4b7`은 provider가 fixture content까지 sensitive로 가려 정제 JSON guard가 거부했다. 원문 JSON을 파일로 저장하지 않고 guard에 pipe하여 해결했다. apply하지 않았다.
- 중간 후보 `p07-260826-f8e2`는 추가 baseline/cleanup 보완 전의 코드 hash라 stale이다. 적용하지 말고 최종 `p07-260826-72bd`만 사용한다.
- 기존 common adapter의 task evidence jq가 배열 원소를 root로 잘못 사용했다. evidence root를 보존하도록 수정하고 원래 식에서 실패하는 회귀 테스트를 추가했다.
- Terraform/HTTP mock 통과는 실제 IAM 수렴과 guest 성공 증거가 아니다. `verify.sh --applied`의 read-only topology와 `execute.sh verify`의 실제 권한 실습을 구분한다.
